# Focused Moment v2.0.7

## 正向计时阶段性目标

- 将容易造成误解的单一提醒改为 25、45、60、90、120 分钟阶段性目标。
- 达到目标时只提醒，不会停止正向计时。
- 旧版保存的 1 分钟提醒会自动使用新的阶段目标，不再在 1 分钟触发。
- 计时页面会显示下一阶段目标；完成全部阶段后明确提示计时仍会继续。

## 验证

- `pnpm check`
- `pnpm build`
- `pnpm test:frontend`（8 项通过）
- `cargo fmt --check`
- `cargo test`（9 项通过）
