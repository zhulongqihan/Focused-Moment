# MAINT-02 工作区清理清单（清理前基线）

> 记录时间：2026-09-11 20:16:15 +08:00
>
> 本清单先于清理生成。大小为 PowerShell 对文件长度求和，不代表磁盘分配量。所有路径均已解析到 `F:\Focused Moment` 内；`Focused Moment Backups` 的内容未读取。

## 基线冻结

| 项目 | 清理前事实 |
| --- | --- |
| 项目根目录 | `F:\Focused Moment` |
| 分支 | `main` |
| HEAD | `0bade576f1aa9e48a4f7e783c06ea270078b3b32` |
| `origin/main` | `0bade576f1aa9e48a4f7e783c06ea270078b3b32` |
| 工作区 | `git status --short --branch` 干净；无 untracked 文件 |
| 应用版本 | `2.10.8`；`package.json`、Cargo、Tauri 配置和 runtime 一致 |
| GitHub remote | `git@github.com:zhulongqihan/Focused-Moment.git` |
| Git 计数 | tracked 283；untracked 0；ignored 121,359 |
| 发布保护 | `v2.10.8` Release 正式存在，四项资产均 `uploaded`；tag 未移动；最近 `main` Checks `34594860391`、macOS Native Smoke `34594860252` 均成功 |
| 工具链 | Node `v24.10.0`；pnpm `10.20.0`；Rust/Cargo `1.94.1`；gh `2.89.0` |
| 进程处理 | 已关闭确认属于本项目的 Vite/pnpm dev 进程；检测到的 Playwright CLI 守护进程未关闭、未清理其状态目录 |

## 体量快照

| 路径 | 文件数 | 文件字节 | 分类 | 处理决定 |
| --- | ---: | ---: | --- | --- |
| `src-tauri/target/` | 76,091 | 84,828,016,661（约 79.002 GiB） | A：Cargo/Tauri 可再生构建物 | `ARCHIVE`；已移至 `F:\Focused Moment Maintenance Archive 20260911-2016\target`，由 Cargo/Tauri 重建 |
| `src-tauri/target/debug/` | 68,239 | 77,611,898,280（约 72.282 GiB） | A：debug 构建缓存 | 随父目录归档 |
| `src-tauri/target/release/` | 7,735 | 7,207,506,154（约 6.713 GiB） | A：release 构建缓存 | 随父目录归档；根目录 v2.10.8 包另行保留 |
| `src-tauri/target/aarch64-apple-darwin/` | 113 | 8,264,727 | A：跨平台构建缓存 | 随父目录归档；macOS workflow 可重建 |
| `src-tauri/target/flycheck0/` | 2 | 344,869 | A：Cargo 临时检查缓存 | 随父目录归档 |
| `src-tauri/target-hotfix/` | 4,849 | 5,212,236,985（约 4.854 GiB） | A：带 Cargo 标记的临时 target | `ARCHIVE`；已移至外部归档，无源码/脚本引用，可由 Cargo 重建 |
| `src-tauri/target-checkaYyf1y/` | 1 | 177 | A：Cargo 临时 target 标记目录 | `ARCHIVE`；已移至外部归档 |
| `src-tauri/targetOmfbiC/` | 1 | 177 | A：Cargo 临时 target 标记目录 | `ARCHIVE`；已移至外部归档 |
| `node_modules/` | 16,393 | 144,419,398（约 137.7 MiB） | A：pnpm 依赖安装物 | `ARCHIVE`；已移至外部归档，由 `pnpm install --frozen-lockfile` 重建 |
| `dist/` | 14 | 14,730,658（约 14.0 MiB） | A：Vite 前端构建物 | `ARCHIVE`；已移至外部归档，由 `pnpm build` 重建 |
| `output/playwright/` | 84 | 86,305,122（约 82.3 MiB） | B：Playwright 截图/页面证据 | `KEEP`；被 QA 文档作为历史证据引用 |
| `output/qa/` | 634 | 294,411,596（约 280.8 MiB） | B：QA 截图、几何和原生证据 | `KEEP`；被 QA 文档引用，不能按大小删除 |
| `output/generate-visual-diffs.mjs` | 1 | 4,362 | B/E：视觉证据辅助脚本 | `KEEP`；输入/输出路径与现有截图证据相关 |
| `test-results/` | 1 | 45 | A：Playwright 临时最后一次状态 | `ARCHIVE`；已移至外部归档，CI/本地测试会重新生成 |
| `.playwright-cli/` | 142 | 42,924,853（约 40.9 MiB） | B/F：交互式浏览器状态和日志 | `UNKNOWN/KEEP`；存在活动守护进程，暂不触碰 |

## 保护项与引用结论

| 路径/对象 | 类型 | 证据与决定 | 恢复方式 |
| --- | --- | --- | --- |
| `Focused Moment Backups/` | D：用户备份 | 根目录内 2 个文件；只确认目录、属性和数量，未读取内容；不删除、不移动、不覆盖 | 用户原路径保留；应用备份/恢复流程可继续使用 |
| `.release/` | C/E：发布元数据和导出 manifest | `scripts/publish-release.ps1` 读取 `artifacts.debug.json`/`artifacts.release.json`；release manifest 当前指向 v2.10.8；全部保留 | 由现有发布/导出流程继续使用 |
| 根目录 `*.exe` | C：当前/历史用户可执行文件 | 当前 v2.10.8、旧 v2.10.7、无版本副本均保留；README 和发布脚本涉及当前包 | 使用现有文件；重新打包会生成新的版本化包 |
| `public/theme-previews/` | E：运行时主题预览 | 5 个预览图由主题代码引用；与概念图同哈希不代表用途相同 | 构建从仓库资源复制 |
| `docs/qa/` 与 `docs/v*/` | B：可追溯 QA/Release 证据 | 已跟踪的当前证据必须保留；README/计划引用的历史路径不因本地生成物清理而改写 | 从 Git checkout 恢复 |
| `src-tauri/icons/` | C/E：Tauri bundle 图标 | Tauri 配置显式列出桌面图标；平台图标不按重复哈希删除 | 从 Git checkout 恢复 |
| `public/vite.svg`、`public/tauri.svg` | E：tracked 脚手架候选 | 当前全仓库源码/配置/脚本/README 引用扫描未命中；不在 MAINT-02 删除，留给 MAINT-03 复核并在删除后完整构建验证 | Git 可恢复 |
| `docs/` 下 138 个 ignored 文件 | F/B：历史、分析和旧版本资料 | 其中 109 个 Markdown；来源和追溯价值各不相同，本阶段不按文件名/年龄批量删除 | 原路径保留；后续逐项归档审计 |
| `src-tauri/gen/schemas/` | A/E：Tauri 自动生成 schema | 由 `src-tauri/.gitignore` 标记为 Generated by Tauri；体量约 291 KiB，但暂不影响主要空间，先保留以免影响 IDE 自动补全 | `pnpm tauri dev/build` 可重新生成 |

## 清理判定和验证门槛

- 本次允许处理的精确目录仅为：`src-tauri/target`、`src-tauri/target-hotfix`、`src-tauri/target-checkaYyf1y`、`src-tauri/targetOmfbiC`、`node_modules`、`dist`、`test-results`；它们已全部移至外部归档 `F:\Focused Moment Maintenance Archive 20260911-2016`，未做不可逆删除。
- 不使用文件年龄、名称、大小或哈希作为单独删除理由；每个允许目录均已确认是被忽略的生成物，且有现成重建命令/锁文件。
- `output/`、`.playwright-cli/`、`.release/`、根目录安装包、主题预览、`docs/` 历史资料和 `Focused Moment Backups/` 本次不清理。
- 归档后必须执行 `pnpm install --frozen-lockfile`、`pnpm check`、`pnpm build`、前端回归、Cargo fmt/check/test、`git diff --check`，并复核应用启动/备份保护。
- 若重建或关键验证失败，停止后续清理，保留失败日志，不用删除测试或降低验收标准掩盖问题。

## 清理后重建与验证记录

> 更新时间：2026-09-11；以下结果来自归档后的当前工作区。首次并行前端回归曾被用户中断且没有退出码，未计入通过；随后按单 worker 完整重跑并取得退出码 0。

| 项目 | 清理后事实 |
| --- | --- |
| 外部归档 | `F:\Focused Moment Maintenance Archive 20260911-2016`；七个精确目录均可在该处找到，工作区原始目录已按重建规则重新生成或保持缺失 |
| 依赖重装 | `pnpm install --frozen-lockfile`：成功，lockfile 未变更 |
| 类型检查 | `pnpm check`：退出码 0 |
| 前端构建 | `pnpm build`：退出码 0；Vite 2062 modules transformed |
| 前端回归 | `pnpm test:frontend -- --workers=1`：67 passed，0 failed，退出码 0，约 3.9 分钟 |
| Rust 格式 | `cargo fmt --check --manifest-path src-tauri/Cargo.toml`：退出码 0 |
| Rust 编译 | `cargo check --locked --manifest-path src-tauri/Cargo.toml`：退出码 0 |
| Rust 测试 | `cargo test --locked --manifest-path src-tauri/Cargo.toml`：33 passed，0 failed；doc-tests 0，退出码 0 |
| Git 差异检查 | `git diff --check`：退出码 0；仅报告 Git 将 LF 转换为 CRLF 的提示 |
| 当前重建体量 | `node_modules/` 16,345 文件/144,357,978 字节；`dist/` 14 文件/14,730,658 字节；`src-tauri/target/` 5,559 文件/3,794,837,736 字节；`test-results/` 1 文件/45 字节 |
| 保护核对 | `Focused Moment Backups/` 仍在原路径，2 个文件，仅核对属性/数量；`output/`、`.playwright-cli/`、`.release/`、根目录 v2.10.8/旧版本 EXE 未纳入本次清理 |

结论：MAINT-02 的可再生目录清理、依赖重建、前端与 Rust 验证门槛已满足；应用启动复核仍作为 MAINT-06/后续原生验证单独记录，不能从构建通过推断启动行为已验收。

## MAINT-06 干净 checkout 验证

| 项目 | 结果 |
| --- | --- |
| checkout | 外部 detached worktree `F:\Focused Moment Clean Checkout 20260911-2028`，提交 `ae6ac45`，初始无未提交变更 |
| 干净重建 | `pnpm install --frozen-lockfile`、`pnpm check`、`pnpm build`：均退出码 0；Vite 2062 modules transformed |
| 干净前端回归 | `pnpm test:frontend -- --workers=1`：67 passed，0 failed，退出码 0，约 4.4 分钟 |
| 干净 Rust 验证 | `cargo fmt --check`、`cargo check --locked`、`cargo test --locked` 均退出码 0；33 passed，doc-tests 0 |
| 干净打包 | `pnpm tauri build --debug`：成功生成 `target/debug/focused-moment.exe`、x64 MSI 和 NSIS bundle |
| 原生启动 | 使用隔离 `F:\Focused Moment Native Smoke Data 20260911-2028` 注入 `LOCALAPPDATA`；进程启动后 `HasExited=False`、真实 `MainWindowHandle=983426`、标题为 `Focused Moment`，人工 `CloseMainWindow` 后退出；退出码 `-1` 仅记录为关闭路径，不把它解释为独立的正常退出码验收 |
| 应用数据隔离 | 隔离目录生成了独立 `FocusedMoment/focused-moment-state.json` 与 `focused-moment-runtime.json`；没有在隔离目录创建备份文件夹 |
| 原工作区保护 | `F:\Focused Moment\Focused Moment Backups` 仍为 2 个文件；未读取或修改内容；根目录 v2.10.8/旧版本 EXE、`.release`、`output`、`.playwright-cli` 均未纳入 checkout 清理 |
| 最终状态 | 干净 worktree 在刷新文件索引后 `git status --short --branch` 无变更；两个 SVG 删除候选在该 checkout 构建/启动/页面回归中未引入失败 |

结论：MAINT-06 的“干净 checkout 可重建、应用可启动、页面回归通过、数据/发布保护不受影响”门槛已满足；当前解除维护阶段对 REFINE-13 的阻塞。原生计时浮窗行为仍未修复或验收，不能从本记录推断其生命周期正确。
