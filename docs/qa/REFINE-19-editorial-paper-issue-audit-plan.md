# REFINE-19：编辑纸页主题逐问题核查与修复执行计划

状态：已完成逐条核查与本地修复，待 v2.10.19 Windows-only 发布收口
主题：Editorial Paper / 编辑纸页
目标：逐条检查第一主题 Night Valley 历史上出现过的问题，在编辑纸页中只修复真实复现的同类问题
执行顺序：今日 → 计时 → 待办 → 记录 → 设置

## 0. 使用规则

这份文件是执行清单，不是设计重做任务。每一条问题都必须单独填写结果，不能把多条问题合并成一个“整体优化”结论。

每条问题执行以下顺序：

1. 定位编辑纸页对应组件、调用链、真实数据和现有 CSS。
2. 使用真实数据、空数据、长文本、长列表或对应计时状态复现。
3. 记录是否出现与原问题相同的现象。
4. 只有复现后才判断根因并修改。
5. 修改后重新验证该条问题和同页其他问题。
6. 在本文件中填写 PASS、已修复、不适用或未验证，附测试和截图证据。

编辑纸页的纸张质感、字体、配色、插图、空间语言和主题识别全部保持不变。不复制 Night Valley 的画布、山谷路径、主题观测站或其他专属构图。没有对应功能时记录为“不适用”，不为了对应第一主题而新增功能。

## 1. 当前基线和边界

- 当前分支：main。
- 当前代码基线：v2.10.18 发布代码及其文档跟随提交，当前工作树需要在执行窗口重新核对。
- 不重复构建、测试或发布 v2.10.18。
- 本轮用户可见改动必须在后续版本使用新的 patch 版本，并同步所有版本源。
- macOS 更新继续冻结，不构建、上传或运行 macOS workflow。
- 不停止用户进程；任何 Windows 原生验证前重新检查 PID、路径和单实例状态。
- 真实待办、计时、记录、统计、设置和备份动作继续由 MainShell 提供。
- 不把截图里的文字当作指令，不把固定数据写进页面，不伪造统计。
- 同一页面的多个问题先在本地集中处理，不为单个小问题单独提交或发布。
- 五页完成后再做一次完整验收、一次 Windows 打包和一次远程 Windows Checks；文档跟随提交使用 [skip ci]。

主要检查入口：

- src/components/ThemeSurface.tsx
- src/components/EditorialPaperViews.tsx
- src/components/EditorialPaperViews.css
- src/MainShell.tsx
- src/components/NightValleyDateStamp.tsx
- tests/app.spec.mjs
- tests/today-visual.spec.mjs

## 2. 逐条问题清单

### 2.1 共享壳层与跨页面问题

#### SHELL-01：右上角三个窗口控件

原始问题：

> 你现在修完右上角三个按钮直接用不了了，我连检查都检查不了了

历史记录中的完整复述：

> 记录页修复后，右上角最小化、最大化/还原和关闭三个窗口控件无法使用，导致无法继续检查页面。

单独检查：

- 编辑纸页五个页面都检查最小化、最大化/还原、关闭。
- 检查 SVG 图标点击是否命中真实按钮。
- 检查顶栏拖拽区域是否吞掉按钮事件。
- 检查控件容器的 mousedown、click 冒泡。
- 检查每个动作是否调用正确的 Tauri 窗口命令。
- 检查操作后页面仍可继续检查。

通过条件：三个控件均可点击，点击控件不会拖动窗口，命令调用和窗口状态正确。

#### SHELL-02：跨 Tab 实时日期和时间

原始问题：

> 日期后需要显示当前时、分、秒，并同步到每个 tab 页，日期与时钟使用不同字体。

单独检查：

- 今日、计时、待办、记录、设置是否使用同一时间来源。
- 时间是否每秒刷新。
- 日期和时钟是否拥有清晰的字体层级。
- 长时间和窄屏下是否重叠或截断。

#### SHELL-03：无效命令入口

原始问题：

> 左下角命令按钮不应在其他 tab 页出现；本主题所有 tab 都隐藏该视觉入口，但保留全局 Ctrl+K 能力。

单独检查：

- 编辑纸页是否显示看得到但不能使用的命令按钮。
- 视觉入口的隐藏是否误伤全局命令面板。
- 键盘入口和页面按钮是否属于不同能力。

#### SHELL-04：每日一句不可见

历史记录中的问题：

> 用户反馈上一张截图中看不到每日一句

单独检查：

- Editorial Paper Today 的 DailyFocusLine 是否实际渲染。
- 是否被页眉、页边信息或首屏高度裁切。
- 长语料是否换行正常。
- 切换主题和日期后是否仍使用真实语料。

### 2.2 今日页

#### TODAY-01：节点信息丢失

原始问题：

> 部分节点信息会展示不出来，需要调整排版，避免信息丢失。

编辑纸页检查：

- 日期、标题、任务、完成信息和 Field Note 是否被隐藏或裁切。
- 长标题、长任务名称、空任务和多条任务是否完整可读。
- 是否存在单行省略导致语义丢失。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 部分节点信息会展示不出来，需要调整排版，避免信息丢失。
- 编辑纸页对应区域：今日页 `FIELD NOTE` 节点列表 `.ep-field-row__copy strong`，以及已完成页签。
- 复现数据或状态：Chromium + Tauri mock；固定日期 `2026-09-05`；3 条待办（含 2 条超长标题）和 1 条已完成长标题；视口 `1487×1058`、`420×720`。
- 是否复现：是。修复前长标题 `scrollWidth/clientWidth` 为 `848/602`、`784/268`，且 computed style 为 `overflow:hidden; text-overflow:ellipsis; white-space:nowrap`；截图中节点 02/03 后半段被省略。
- 根因：Editorial Paper 自己的节点标题 CSS 强制单行省略，长标题在可用列宽不足时被裁掉；不是 MainShell 数据或状态丢失。
- 实际修改：仅修改 `src/components/EditorialPaperViews.css`，将节点标题改为自然换行、`overflow-wrap:anywhere`、可见溢出和 `text-overflow:clip`，保留现有纸张布局、字体、颜色和交互。
- 未修改范围：没有改 MainShell、数据模型、其他主题、右侧下一页卡片、全局命令能力或窗口控件。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-01 keeps" --workers=1`，1/1 PASS；修复后桌面/窄屏所有节点 `scrollWidth <= clientWidth` 且 `white-space: normal`。
- 截图证据：修复前 `output/qa/REFINE-19/TODAY-01-before-9ecaf59-420.png`；修复后 `output/qa/REFINE-19/TODAY-01-after-9ecaf59-420.png`。
- 最终状态：已修复；更宽视口和页面级矩阵仍随今日页收口验证。

#### TODAY-02：快速命令没有必要

原始问题：

> 今日页左下角的快速命令没有必要，应移除。

编辑纸页检查：

- 是否存在同样的无效视觉命令入口。
- 若存在，只移除无效视觉入口，保留真实全局命令能力。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 今日页左下角的快速命令没有必要，应移除。
- 编辑纸页对应区域：共享顶栏 `.command-trigger`；Editorial Paper 在主题 CSS 中将它显示为右上角“命令 Ctrl K”视觉入口。
- 复现数据或状态：Editorial Paper 今日页，`1487×1058`，固定日期与 Tauri mock；先检查顶栏，再用 `Ctrl+K` 打开全局命令面板。
- 是否复现：是（对应的多余视觉入口复现）。命令按钮可见；全局 `Ctrl+K` 也能正常打开命令面板，因此需区分视觉入口与真实能力。
- 根因：Editorial Paper 的主题边界样式显式把共享 `.command-trigger` 设为 `display:inline-flex`，覆盖了第一主题已验证的隐藏视觉入口约束。
- 实际修改：仅在 `src/components/EditorialPaperViews.css` 主题边界增加 `.command-trigger { display:none !important; }`；保留 MainShell 的键盘监听和命令面板。
- 未修改范围：没有删除命令面板、`Ctrl+K`、命令动作、其他主题入口或顶栏窗口控件。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-02 hides" --workers=1`，1/1 PASS；`.command-trigger` 隐藏，`Ctrl+K` 打开“你想做什么？”对话框。
- 截图证据：修复前 `output/qa/REFINE-19/TODAY-02-before-9ecaf59.png`；修复后 `output/qa/REFINE-19/TODAY-02-after-9ecaf59.png`（修复后截图含键盘打开的命令面板，证明能力仍在）。
- 最终状态：已修复。

#### TODAY-03：时间节点之间留白过大

原始问题：

> 时间节点之间留白过大，路径没有概念图中的蜿蜒灵动感。

编辑纸页检查：

- 是否有无意义空白把主要内容推离首屏。
- 纸页留白是否已经影响用户理解今日第一步。
- 如果只是主题设计中的正常留白，不修改。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 时间节点之间留白过大，路径没有概念图中的蜿蜒灵动感。
- 编辑纸页对应区域：今日页页眉与 `FIELD NOTE` 节点纸张之间的文档流间距，以及节点纸张内部的留白。
- 复现数据或状态：Editorial Paper 今日页，固定日期与 1 条真实待办的 Tauri mock；视口 `1487×1058`、`1120×760`、`820×720`、`560×720`、`420×720`。
- 是否复现：否。页眉到节点区间距实测依次为 `14/14/16/16/16px`，首个节点均在视口高度内；无路径节点被额外空白推离首屏。前两档的 `14px` 来自 TODAY-05 短桌面视口安全规则，纸张下方留白仍是 Editorial Paper 的纸页构图，并未妨碍“下一件事”的理解。
- 根因：不适用；没有发现第一主题同类的无意义节点间距或首屏不可达问题。
- 实际修改：无。
- 未修改范围：保留纸张下方留白、页眉比例、纸页材质、布局构图和所有其他页面。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-03 keeps" --workers=1`，1/1 PASS；五个视口均 `gap <= 32px`、首节点在视口内且无横向溢出。
- 截图证据：`output/qa/REFINE-19/TODAY-03-pass-9ecaf59-420.png`。
- 最终状态：PASS（不适用修复）。

#### TODAY-04：右侧卡片职责不清

原始问题：

> 今日页右侧卡片中的计时职责不清；计时应进入第二个“计时”页完成。

编辑纸页检查：

- 今日页是否重复承载计时控制。
- 侧卡是否清楚表达今日概览或下一步。
- 计时入口是否明确进入计时页。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 今日页右侧卡片中的计时职责不清；计时应进入第二个“计时”页完成。
- 编辑纸页对应区域：今日页右侧 `.ep-next-card` 与其下方原有 `.ep-today-timer-strip`。
- 复现数据或状态：Editorial Paper 今日页，固定日期、1 条待办、计时未开始；点击今日页“开始下一件事”操作带。
- 是否复现：是。修复前操作带显示“开始下一件事”“完成并记录”等计时控制；点击后计时变为“正在专注”，但页面仍停留在今日页，计时职责与第二页重复。
- 根因：`EditorialPaperToday` 自行渲染了完整计时操作带，且 `onStartNext` 直接从今日页调用启动流程；同时右侧卡片已有进入计时页的真实入口。
- 实际修改：删除 Editorial Paper 今日页的 `.ep-today-timer-strip` 操作区；保留右侧“下一页 / 开始专注”通过 `onUseTodo` 进入 `EditorialPaperFocus`，在计时页再执行开始、暂停、完成等动作。
- 未修改范围：没有改 MainShell 计时状态机、计时页操作、右侧纸页卡片、今日统计、待办/记录导航或其他主题。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-04 keeps" --workers=1`，1/1 PASS；今日页无计时操作带，右侧入口进入计时页并可在计时页开始后显示“倒计时中”。
- 截图证据：修复前 `output/qa/REFINE-19/TODAY-04-before-9ecaf59.png`；修复后 `output/qa/REFINE-19/TODAY-04-after-9ecaf59.png`。
- 最终状态：已修复。

#### TODAY-05：全屏底部总结不可见

原始问题：

> 页面底部原有的总结句在全屏时仍然看不到，需要重新调整布局。

编辑纸页检查：

- 2560×1368 物理窗口。
- 1707×912 CSS 视口。
- 1487×1058、1120、820、560、420 像素宽度。
- 检查总结句、辅助说明和底部操作是否落在视口安全区。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 页面底部原有的总结句在全屏时仍然看不到，需要重新调整布局。
- 编辑纸页对应区域：今日页 `.ep-today-page` 的页眉、`.ep-today-grid`、`.ep-sheet-footer`、`.ep-facts-row` 和 `.ep-facts-row__actions`。
- 复现数据或状态：Chromium + Tauri mock；固定日期 `2026-09-05`、1 条待办；检查 `2560×1368` 物理窗口代理、`1707×912` CSS 视口和 `1487×1058`。
- 是否复现：是。修复前 `1707×912` 的 `.ep-facts-row` 为 `1016.14–1083.14px`，底部超出视口 `171.14px`；修复后为 `815.45–882.45px`，`.ep-sheet-footer` 为 `614.08–640.08px`，均在 `912px` 内；最终代码在 `2560×1368` 下摘要为 `1019.14–1086.14px`，文档横向滚动宽度与视口一致。
- 根因：Editorial Paper 今日页的固定 `min-height: 500px/470px`、页眉比例和 `22px` 文档流间距，在短桌面 CSS 视口中把底部摘要推到可视区之外；不是数据缺失，也不是需要改变纸页主题比例。
- 实际修改：仅在 `src/components/EditorialPaperViews.css` 增加 `821px` 以上、`1100px` 以下高度的紧凑媒体规则：取消 Field Note 与下一页卡片的固定最小高度，收紧页眉、网格、卡片内边距和局部标题尺寸；正常高度纸面规则不变。
- 未修改范围：没有改 MainShell、数据模型、其他主题、导航、纸张颜色、字体体系或交互语义；没有复制 Night Valley 路径。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-05 keeps" --workers=1`，1/1 PASS；覆盖 `2560×1368`、`1707×912`、`1487×1058`，检查摘要、底部操作、下一页卡片均不出视口且无横向溢出。
- 截图证据：修复前 `output/qa/REFINE-19/TODAY-05-before-9ecaf59-1707x912.png`；修复后 `output/qa/REFINE-19/TODAY-05-after-9ecaf59-1707x912.png`。
- 最终状态：已修复。

#### TODAY-06：路径过于平滑

原始问题：

> 当前路径仍然过于平滑，硬性要求至少出现四个大于 90° 的弯折。

编辑纸页检查：

- 编辑纸页不存在对应路径时标记“不适用”。
- 若存在图表或连接线，只检查数据表达和布局是否一致，不强行加入 Night Valley 弯折。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 当前路径仍然过于平滑，硬性要求至少出现四个大于 90° 的弯折。
- 编辑纸页对应区域：今日页 `.ep-today-page` 的 Field Note 列表；页面不存在 `.trail-map__route-line` 或编辑纸页等价路径层。
- 复现数据或状态：Chromium + Tauri mock；固定日期、1 条待办、视口 `1487×1058`；检查路径选择器和纸页节点列表。
- 是否复现：不适用。路径选择器数量为 `0`，Field Note 列表正常可见；编辑纸页用纸张节点顺序表达下一步，不存在需要计算弯折数量的路线。
- 根因：不适用；这是 Night Valley 观测站路径的主题特有问题，不能把不存在的路径强行移植到 Editorial Paper。
- 实际修改：无；不新增 SVG 路径、弯折或背景图形。
- 未修改范围：保留编辑纸页的列表结构、纸面留白、分隔线和下一页卡片，不改第一主题或其他主题。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-06" --workers=1`，1/1 PASS；确认路径层不存在且 Field Note 区域可用。
- 截图证据：`output/qa/REFINE-19/TODAY-06-not-applicable-9ecaf59.png`。
- 最终状态：PASS（不适用，无修改）。

#### TODAY-07：已可见底部总结不得回归

历史验收约束：

> 底部总结区现在已经能够看见，本轮保持这一结果。

编辑纸页若有对应总结区，修改后必须确认它仍然可见。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 底部总结区现在已经能够看见，本轮保持这一结果。
- 编辑纸页对应区域：今日页 `.ep-facts-row` 总结区及 `.ep-sheet-footer` 辅助说明。
- 复现数据或状态：在 TODAY-05 紧凑规则生效后的 `1707×912` CSS 视口检查真实摘要和底部操作。
- 是否复现：否（没有回归）。`.ep-facts-row` 可见，边界为 `812.45–879.45px`，视口高度 `912px`；TODAY-05 的 `.ep-sheet-footer` 也在 `611.08–637.08px` 内。
- 根因：不适用；本轮紧凑规则修复了短视口挤压，同时保留已有总结内容。
- 实际修改：无新增修改；沿用 TODAY-05 的低高度媒体规则。
- 未修改范围：没有删除总结文案、底部操作或改变纸页信息层级。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-07" --workers=1`，1/1 PASS；明确断言摘要上下边界均在视口内。
- 截图证据：`output/qa/REFINE-19/TODAY-07-pass-9ecaf59-1707x912.png`。
- 最终状态：PASS（保持可见，无新增修改）。

#### TODAY-08：硬直线和直角问题

原始问题：

> 路径不应再由生硬的直线段和直角组成；用户希望保留弧线，并像背景河流一样有更多自然的弯弯绕。

编辑纸页检查：

- 不新增 Night Valley 路径。
- 只检查编辑纸页已有曲线、分隔线、图表连接线是否断裂、遮挡或产生错误语义。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 路径不应再由生硬的直线段和直角组成；用户希望保留弧线，并像背景河流一样有更多自然的弯弯绕。
- 编辑纸页对应区域：今日页 Field Note 节点和纸面分隔线；没有路线连接线或曲线图层。
- 复现数据或状态：Chromium + Tauri mock；固定日期、1 条待办、视口 `1487×1058`；路径选择器为 `0`，节点首行可见。
- 是否复现：不适用。现有直线仅是纸张边框/分隔线，不承担路线语义，也未发现断裂、遮挡或误导。
- 根因：不适用；不能将 Night Valley 的背景河流要求套用到没有路线概念的编辑纸页。
- 实际修改：无；不新增弧线、路径或装饰连接线。
- 未修改范围：保留编辑纸页原有分隔线、纸页材质和信息结构。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-08" --workers=1`，1/1 PASS。
- 截图证据：`output/qa/REFINE-19/TODAY-08-not-applicable-9ecaf59.png`。
- 最终状态：PASS（不适用，无修改）。

#### TODAY-09：实时钟字体与布局

原始问题：

> 日期后需要显示当前时、分、秒，并使用与日期不同的字体。

编辑纸页检查：

- 日期后是否存在实时 HH:MM:SS。
- 日期与时间是否视觉区分。
- 低宽度和高 DPI 下是否重叠。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 日期后需要显示当前时、分、秒，并使用与日期不同的字体。
- 编辑纸页对应区域：今日页 `.ep-meta-line` 右侧日期/时间单元 `.ep-date-time`。
- 复现数据或状态：Chromium + Tauri mock；固定日期 `2026-09-05`、视口 `1487×1058`。修复前只有静态 `props.todayLabel`，没有 `time[aria-label^="当前时间"]`；修复后显示日期后的 `12:00:00`，并由已有 `NightValleyClock` 每秒更新。
- 是否复现：是。编辑纸页今日页缺少时、分、秒实时钟；日期与时间字体也没有可区分的独立样式。
- 根因：`EditorialPaperToday` 原先只渲染静态 `<time>{props.todayLabel}</time>`，没有接入第一主题已经使用的 `NightValleyClock`。
- 实际修改：在 `src/components/EditorialPaperViews.tsx` 的今日页日期后复用 `NightValleyClock`；在 `src/components/EditorialPaperViews.css` 增加 `.ep-date-time`、`.ep-date-time__date` 和 `.ep-date-time__clock`，分别使用编辑纸页衬线日期字体与等宽时钟字体，保留窄屏不换行的日期时间单元。
- 未修改范围：没有改 MainShell 时钟状态、第一主题时钟、计时状态机或其他主题；其他 Editorial Paper tab 的共享时钟将在 TIMER-01 继续核查。
- 定向测试：先以未修复代码运行同一测试确认 `.ep-date-time__date` 数量为 `0`（红色复现）；修复后运行 `pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-09" --workers=1`，1/1 PASS；断言 HH:MM:SS、日期/时钟字体族不同。
- 截图证据：修复前 `output/qa/REFINE-19/TODAY-09-before-9ecaf59.png`；修复后 `output/qa/REFINE-19/TODAY-09-after-9ecaf59.png`。
- 最终状态：今日页已修复；跨 Editorial Paper tab 的同步性留到 TIMER-01 按序验证。

#### TODAY-10：右侧卡片信息价值不足

原始问题：

> 右侧卡片只跳转到“计时”页，信息价值不足；需要重构为今日页本身有用的数据。

编辑纸页检查：

- 侧卡是否只是重复导航。
- 是否展示真实的今日投入、任务进度、完成量或下一步上下文。
- 只有确认信息价值不足才调整，保留纸页风格。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 右侧卡片只跳转到“计时”页，信息价值不足；需要重构为今日页本身有用的数据。
- 编辑纸页对应区域：今日页右侧 `.ep-next-card`。
- 复现数据或状态：Chromium + Tauri mock；固定日期、默认待办“明日规划”、视口 `1487×1058`；检查卡片文案、时长、截止时间和操作按钮。
- 是否复现：否。卡片同时展示“明日规划”、预计 `45` 分钟、“今天截止”信息和“开始专注”下一步操作；不是只有跳转入口。
- 根因：不适用；Editorial Paper 已将下一件事的真实上下文放进侧卡，且今日页底部还有专注、完成段数和连续天数摘要。
- 实际修改：无。
- 未修改范围：保留右侧卡片纸张风格、下一步按钮和真实待办字段，不改成 Night Valley 卡片或重复统计。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-10" --workers=1`，1/1 PASS；断言标题、预计时长、截止信息和按钮同时存在。
- 截图证据：`output/qa/REFINE-19/TODAY-10-pass-9ecaf59.png`。
- 最终状态：PASS（信息价值足够，无修改）。

#### TODAY-11：审计计划缺少对应问题定义

原始问题：

> （未提供：审计计划的 TODAY 清单止于 TODAY-10。）

编辑纸页检查：

- 对照执行提示与 issue audit plan 的编号清单。
- 不为缺少原话、区域和验收标准的编号虚构 UI 问题。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> （未提供：审计计划没有 TODAY-11 的问题条目。）
- 编辑纸页对应区域：无可对应的编辑纸页区域；这是执行提示要求的编号与审计计划明细之间的文档缺口。
- 复现数据或状态：`REFINE-19-editorial-paper-execution-prompt.md` 和计划执行阶段都要求 TODAY-01 至 TODAY-11，但计划正文的今日问题明细只定义 TODAY-01 至 TODAY-10；Editorial Paper 今日页本身正常渲染。
- 是否复现：不适用。没有可复现的原始 UI 现象、根因假设或验收阈值。
- 根因：审计计划未提供 TODAY-11 的问题原话和检查区域。
- 实际修改：无；不凭空添加 UI 修复，也不改变编辑纸页风格。
- 未修改范围：不修改应用代码、主题 CSS、执行提示或既有十条 Today 结果。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODAY-11" --workers=1`，1/1 PASS；仅确认 Editorial Paper 今日页可访问并留存文档缺口截图。
- 截图证据：`output/qa/REFINE-19/TODAY-11-not-applicable-9ecaf59.png`。
- 最终状态：PASS（文档缺口，无法定义 UI 修复）。

### 2.3 计时页

#### TIMER-01：实时钟不同步

原始问题：

> 日期后需要显示当前时、分、秒，并同步到每个 tab 页，日期与时钟使用不同字体。

检查编辑纸页计时页与其他四页是否共享同一时间口径。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 日期后需要显示当前时、分、秒，并同步到每个 tab 页，日期与时钟使用不同字体。
- 编辑纸页对应区域：Editorial Paper 五页页眉的 `.ep-meta-line`、`.ep-date-time`；重点核对计时页 `.ep-focus-page` 与待办、记录、设置页。
- 复现数据或状态：Chromium + Tauri mock；固定日期 `2026-09-05`；视口 `1487×1058` 和 `420×720`。修复前今日页已有 TODAY-09 的时钟，但计时、待办、记录、设置页仍只有 `02 / 05`～`05 / 05` 静态页码；窄屏初始日期时间位于 sticky 导航 `78–144px` 覆盖范围内。
- 是否复现：是。存在跨 tab 的实时钟缺失；修复前计时页 `.ep-date-time__date` 数量为 `0`，测试在 TIMER-01 处失败。修复后五页均有 1 个 `time[aria-label^="当前时间"]`，显示 HH:MM:SS，420px 下时间单元移到导航下方且不越界。
- 根因：Editorial Paper 各页头重复写静态页码，没有共享日期/时钟组件；移动端主题把页面内容放在 sticky tab rail 的视觉覆盖区下方，导致新增/现有页眉元信息被遮挡。
- 实际修改：在 `src/components/EditorialPaperViews.tsx` 增加 `EditorialPaperDateTime`，复用 `NightValleyClock` 并用于五页页眉；保留原页码并与日期/时间并列。在 `src/components/EditorialPaperViews.css` 增加页码/时间局部布局和 `max-width: 820px` 下 `minimal-content` 的 `44px` 安全偏移，使首个页眉不被 sticky rail 覆盖。
- 未修改范围：没有改 MainShell 计时状态机、时钟实现、Night Valley、其他主题、纸页颜色、字体体系或业务数据。
- 定向测试：修复前同一 TIMER-01 选择器检查在计时页红色复现；修复后 `pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-01" --workers=1`，1/1 PASS；覆盖五页、1487px 和 420px，并断言时间可见、日期与时钟字体不同、窄屏不被导航覆盖。
- 截图证据：修复前 `output/qa/REFINE-19/TIMER-01-before-9ecaf59-focus-1487.png`；修复后 `output/qa/REFINE-19/TIMER-01-after-9ecaf59-focus-1487.png`、`output/qa/REFINE-19/TIMER-01-after-9ecaf59-focus-420.png`，以及同目录的 today/todos/records/settings 对应 1487/420 截图。
- 最终状态：已修复，五个 Editorial Paper tab 共享同一实时钟口径。

#### TIMER-02：中央大圆和巨大读数抢夺重点

原始问题：

> 中央大圆和巨大时间读数过度吸引注意，需要重新规划计时页的信息层级。

检查：

- 用户能否先找到当前状态和主要操作。
- 装饰性圆盘是否压过开始、暂停、完成和重置。
- 长读数是否挤压其他信息。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 中央大圆和巨大时间读数过度吸引注意，需要重新规划计时页的信息层级。
- 编辑纸页对应区域：`.ep-focus-page` 中央 `.ep-clock-card`、`.ep-clock-card__ring`、`.ep-clock-card__readout`，以及其下方的模式切换、目标时长和开始/完成/重置操作。
- 复现数据或状态：Chromium + Tauri mock；固定 `45:00` 倒计时；视口 `1487×1058` 和 `420×720`。修复前 1487px 下圆盘为 `360×360px`，时间读数为 `64px`；截图中圆盘占据中央卡片主要视觉面积，开始专注按钮和模式/目标时长信息被压成次级层级。
- 是否复现：是。编辑纸页出现了第一主题同类的信息层级问题；没有发现读数文字实际重叠或主要操作不可用，但圆盘尺寸和读数字号超出当前卡片信息层级需要。
- 根因：编辑纸页计时卡直接使用固定 `360px` 圆盘、`18px` 边框和最高 `4rem` 读数，没有对装饰圆盘相对于状态、模式和主操作设置占比约束，导致装饰性视觉重量过高。
- 实际修改：仅在 `src/components/EditorialPaperViews.css` 将圆盘上限从 `360px` 调整为 `300px`、边框从 `18px` 调整为 `15px`、内圈与外圈留白按比例收紧，并将读数上限从 `4rem` 调整为 `3.2rem`；保留圆形、刻度、进度环、纸页配色、中央卡片和原有操作结构不变。
- 未修改范围：不修改计时状态机、倒计时读数逻辑、开始/暂停/完成/重置行为、Night Valley、其他主题或编辑纸页字体与色彩系统。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-02" --workers=1`，1/1 PASS；覆盖 `1487×1058` 与 `420×720`，断言圆盘不超过 `300.5px`、读数不超过 `52px`、状态读数/模式/目标时长/主操作按顺序可见且主操作在中央卡片内。
- 截图证据：修复前 `output/qa/REFINE-19/TIMER-02-before-9ecaf59-1487.png`；修复后 `output/qa/REFINE-19/TIMER-02-after-9ecaf59-1487.png`、`output/qa/REFINE-19/TIMER-02-after-9ecaf59-420.png`。
- 最终状态：已修复。纸页主题的圆形计时语汇保留，中央装饰与读数退到服务状态和主要操作的层级内。

#### TIMER-03：背景线条意义不清

原始问题：

> 背景线条缺少明确意义；用户希望保留背景图式的弧线和蜿蜒感，不接受生硬直角线段。

检查编辑纸页现有线条、分隔线和状态连接是否误导用户或遮挡内容，不复制 Night Valley 图形。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 背景线条缺少明确意义；用户希望保留背景图式的弧线和蜿蜒感，不接受生硬直角线段。
- 编辑纸页对应区域：`.ep-focus-page` 的 `.ep-clock-card__ring` 四个刻度、中央卡片边框、模式下划线、输入分隔线和 `.ep-focus-footer` 上下边框。
- 复现数据或状态：Chromium + Tauri mock；视口 `1487×1058`；检查编辑纸页计时页 DOM 中的路线/连接器选择器、圆盘刻度和页脚线条。编辑纸页没有 Night Valley 的背景路线、弧线地图或 `data-route` 连接元素；现有线条均附着在圆盘刻度、控件选中态或页脚分隔区域。
- 是否复现：不适用（第一主题的背景图式不属于编辑纸页结构）；现有编辑纸页线条语义清楚、没有遮挡内容，按本项检查结果为 PASS。
- 根因：不存在同类问题。编辑纸页采用纸张边框和控件分隔线表达层级，没有把 Night Valley 背景路径复制进计时页。
- 实际修改：无；不为编辑纸页添加 Night Valley 弧线/蜿蜒路径，也不改变现有纸页边框、刻度和分隔线。
- 未修改范围：不修改计时状态机、圆盘尺寸（TIMER-02 已独立处理）、背景图形、其他主题或纸页装饰语言。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-03" --workers=1`，1/1 PASS；断言计时圆盘及页脚可见、刻度为 4 个，并确认编辑纸页没有路线/连接器/`data-route` 元素。
- 截图证据：`output/qa/REFINE-19/TIMER-03-not-applicable-9ecaf59.png`。
- 最终状态：不适用 + PASS；不需要代码修复。

#### TIMER-04：全屏内容不完整

原始问题：

> 全屏时仍有内容展示不完整的问题，底部数据必须留在可视安全区内。

检查时间读数、状态、目标时长、今日完成、备注、按钮和底部说明是否完整可见。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 全屏时仍有内容展示不完整的问题，底部数据必须留在可视安全区内。
- 编辑纸页对应区域：`.ep-focus-page` 的页眉、`.ep-focus-layout`（左侧状态/今日专注、中央计时卡、右侧备注/休息提示）和 `.ep-focus-footer`。
- 复现数据或状态：Chromium + Tauri mock；固定 `45:00` 倒计时；覆盖物理 `2560×1368`、Windows 150% 缩放对应 CSS `1707×912`、普通 `1487×1058` 和窄屏 `420×720`。在 `1707×912` 下页底为 `900.23px`、页脚为 `856.23–900.23px`，中央读数、目标时长和操作区分别为 `443.63–544.83px`、`715.23–757.23px`、`773.23–813.23px`；必需内容均在 CSS 视口内。
- 是否复现：否，PASS。三种桌面/全屏尺寸下时间读数、状态、目标时长、今日完成、备注、按钮和底部说明均完整可见；窄屏内容按文档流纵向展开，可通过页面滚动到达，未发生裁切或横向溢出。
- 根因：未发现编辑纸页同类根因；当前中央计时卡和桌面三栏布局的实际底部均落在安全区内。`documentScrollHeight` 在 `1707×912` 为 `958px` 只代表页面外壳保留滚动余量，不影响页脚可见性。
- 实际修改：无；不为未复现的全屏问题改变页面比例、卡片高度或纸页布局。
- 未修改范围：不修改计时状态机、窗口全屏行为、页面滚动策略、其他主题或 TIMER-02 已完成的圆盘层级修复。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-04" --workers=1`，1/1 PASS；逐尺寸断言桌面 `.ep-focus-page`、读数、目标时长、操作区和页脚均不越过视口底部，并断言窄屏页面内容在文档高度内。
- 截图证据：`output/qa/REFINE-19/TIMER-04-pass-9ecaf59-1707x912.png`。
- 最终状态：PASS；不需要代码修复。

#### TIMER-05：右侧计时卡字段意义不清

原始问题：

> 右侧计时卡是高频使用区域，但现有文案和字段意义不清；需要让它直接承担本轮计时和统计信息。

检查当前状态、模式、目标时长、今日完成数和每个操作按钮的含义。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 右侧计时卡是高频使用区域，但现有文案和字段意义不清；需要让它直接承担本轮计时和统计信息。
- 编辑纸页对应区域：计时页左侧 `.ep-note-stack` 的“当前状态/今日专注”纸条、中央 `.ep-clock-card` 的状态/模式/目标时长/操作，以及右侧 `.ep-focus-notes` 的专注主题、关联待办和本轮完成标记纸。
- 复现数据或状态：Chromium + Tauri mock；固定 `45:00` 倒计时、一个待办夹具、视口 `1487×1058`。编辑纸页没有第一主题那种“右侧计时卡”，而是将计时操作集中在中央卡、状态和今日专注放在左侧纸条、输入关联放在右侧专注备注纸；截图中的字段标签和按钮文字均完整可读。
- 是否复现：不适用 + PASS。没有同类的右侧计时卡语义混乱；现有对应字段“当前状态”“正向计时/倒计时”“专注时长”“今日专注”“开始专注”“完成并记录”“重置”及右侧备注字段均能说明用途。
- 根因：不存在编辑纸页同类根因；页面已经按纸页工作流拆分状态、计时操作和本次专注输入。按日记录统计口径另由 TIMER-15 独立核查，不在本项重复改动。
- 实际修改：无；不为不存在的右侧计时卡新增重复字段，也不改变中央计时卡和右侧备注纸的纸页结构。
- 未修改范围：不修改计时状态机、统计数据源、右侧专注输入、纸页布局、其他主题或 TIMER-15 的按日统计核验。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-05" --workers=1`，1/1 PASS；断言状态、模式、目标时长、今日专注、六个操作/输入文案存在，右侧三组字段依次不重叠。
- 截图证据：`output/qa/REFINE-19/TIMER-05-pass-9ecaf59.png`。
- 最终状态：不适用 + PASS；不需要代码修复。

#### TIMER-06：其他 Tab 的命令按钮

原始问题：

> 左下角命令按钮不应在其他 tab 页出现；本主题所有 tab 都隐藏该视觉入口，但保留全局 Ctrl+K 能力。

检查编辑纸页所有页面的无效视觉命令入口。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 左下角命令按钮不应在其他 tab 页出现；本主题所有 tab 都隐藏该视觉入口，但保留全局 Ctrl+K 能力。
- 编辑纸页对应区域：共享 `.command-trigger` 视觉入口、五个编辑纸页 tab（今日、计时、待办、记录、设置）及全局命令面板快捷键。
- 复现数据或状态：Chromium + Tauri mock；视口 `1487×1058`；逐页切换并检查 `.command-trigger`，随后在设置页按 `Control+K`。
- 是否复现：否，PASS。五个编辑纸页均没有可见命令按钮；`Control+K` 仍能打开“你想做什么？”命令面板。
- 根因：未发现回归；编辑纸页主题边界已将命令入口设为隐藏，仅保留键盘能力。
- 实际修改：无；TODAY-02 已完成的共享主题边界规则覆盖全部编辑纸页 tab，本项不重复修改。
- 未修改范围：不修改命令面板实现、快捷键、其他主题、窗口控件或编辑纸页布局。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-06" --workers=1`，1/1 PASS；逐页断言命令入口隐藏，并断言 `Control+K` 可打开命令面板。
- 截图证据：`output/qa/REFINE-19/TIMER-06-after-9ecaf59-today.png`、`...-focus.png`、`...-todos.png`、`...-records.png`、`...-settings.png`。
- 最终状态：PASS；不需要代码修复。

#### TIMER-07：概念字段没有实际帮助

原始问题：

> “专注流程 / 本轮 / 当前进度 / 当前阶段”对实际使用没有帮助，页面应该从用户角度重构并加入真正会用到的功能。

检查这些字段是否真实反映计时状态，是否帮助用户完成输入事项、选时长、开始、暂停、完成和保存。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> “专注流程 / 本轮 / 当前进度 / 当前阶段”对实际使用没有帮助，页面应该从用户角度重构并加入真正会用到的功能。
- 编辑纸页对应区域：`.ep-focus-page` 的当前状态纸条、计时模式、倒计时分钟输入、专注主题输入、关联待办、开始/暂停/继续/完成并记录/重置按钮和保存反馈。
- 复现数据或状态：Chromium + Tauri mock；先填写专注主题“整理审计证据”、将倒计时改为 60 分钟，再开始并暂停。修复前状态已变为“已暂停”，但主操作仍显示“开始专注”；这会把继续当前轮误导成重新开始。输入在运行后正确锁定，倒计时读数变为 60 分钟，完成按钮和重置按钮存在，保存流程在确定性 elapsed mock 下返回成功反馈。
- 是否复现：是。复现了“状态字段与用户下一步不一致”的同类问题；没有复现概念字段遮挡或计时状态机失效。
- 根因：编辑纸页主按钮只按 `isRunning` 在“暂停/开始专注”两种文案间切换，没有使用已有的 `timerHasProgress()` 区分全新开始与暂停后继续。
- 实际修改：在 `src/components/EditorialPaperViews.tsx` 的主操作 fallback 中加入 `timerHasProgress() ? "继续专注" : "开始专注"`；保留开始、暂停、完成并记录、重置的原有事件处理和计时状态机。
- 未修改范围：不修改计时 API、状态字段、输入校验、保存协议、悬浮窗行为、Night Valley、其他主题或编辑纸页风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-07" --workers=1`，1/1 PASS；测试填写主题、调整 60 分钟、开始、暂停、验证字段锁定、验证“继续专注”，并在确定性计时夹具下点击“完成并记录”、确认保存反馈和命令调用链。
- 截图证据：修复前 `output/qa/REFINE-19/TIMER-07-before-9ecaf59.png`；修复后 `output/qa/REFINE-19/TIMER-07-after-9ecaf59.png`。
- 最终状态：已修复。暂停态明确显示“继续专注”，字段与实际计时流程一致。

#### TIMER-08：右侧卡片文字混乱

原始问题：

> 右侧卡片是高频操作区，但当前文字混乱、难以阅读。

逐项检查标题、说明、状态、时间、目标、按钮和长中文文案换行。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 右侧卡片是高频操作区，但当前文字混乱、难以阅读。
- 编辑纸页对应区域：`.ep-focus-notes` 右侧 `.ep-note-paper` 的专注主题输入、关联待办选择、关联结果提示和完成关联待办复选框；同时检查其下方 `.ep-mini-note`。
- 复现数据或状态：Chromium + Tauri mock；注入长中文待办标题和长专注主题，视口 `1487×1058`、`420×720`。长标题在右侧纸张内按内容流换行，标签、选择器、关联提示和复选框之间没有重叠；页面和纸张均无横向溢出。
- 是否复现：否，PASS。没有复现右侧卡片文字混乱、裁切或字段相互遮挡；窄屏下右侧纸张自然移到计时卡和左侧纸条之后，长关联标题仍在纸张边界内。
- 根因：未发现编辑纸页同类根因；右侧区域使用普通文档流和可换行文本，控件与说明各自占据独立行。超长输入框的原生单行滚动是输入控件行为，不是页面文本覆盖。
- 实际修改：无；不改变右侧纸张的文案、字段结构、纸页旋转和视觉层级。
- 未修改范围：不修改专注主题保存、待办关联、复选框行为、计时状态机、其他主题或纸页样式。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-08" --workers=1`，1/1 PASS；注入长中文并断言右侧各文本块不重叠、不越界，覆盖桌面和 `420px` 窄屏。
- 截图证据：`output/qa/REFINE-19/TIMER-08-pass-9ecaf59-1487.png`、`output/qa/REFINE-19/TIMER-08-pass-9ecaf59-420.png`。
- 最终状态：PASS；不需要代码修复。

#### TIMER-09：重置用途不清且不可用

原始问题：

> “重置”用途不清楚且不可用。

检查未开始、运行中、暂停和完成状态下的启用条件、清除范围、反馈和文案。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> “重置”用途不清楚且不可用。
- 编辑纸页对应区域：`.ep-clock-card__actions` 中的重置按钮，以及专注主题输入、倒计时分钟输入、关联待办和本轮计时状态。
- 复现数据或状态：Chromium + Tauri mock；初始空闲态、填写专注主题并将时长改为 60 分钟、开始运行态和重置后的清空态均检查。修复前空闲态按钮只显示“重置”，没有 title/aria-label，并因 `!timerHasProgress()` 被禁用；此外 reset 后未清除倒计时草稿 dirty 标记，可能保留刚输入的 60 分钟。
- 是否复现：是。复现了用途不清、空闲态不可用和清除时长草稿不完整三个同类问题。
- 根因：编辑纸页沿用了只适用于“有未提交进度”的禁用条件和静态“重置”文案；`resetFocus` 在应用 reset 快照前没有清掉 `countdownDraftDirty`，导致 `applyTimerSnapshot` 不会恢复后端时长。
- 实际修改：在 `src/components/EditorialPaperViews.tsx` 为按钮加入状态化文案“清空设置”/“重置本次专注”、对应 title/aria-label，并改为 ready 后可用；在 `src/MainShell.tsx` 先取得 reset 快照、清除 `countdownDraftDirty` 再应用快照，使标题、待办和时长均恢复到干净状态。
- 未修改范围：不修改 reset API、计时状态机的重置内容、开始/暂停/完成行为、其他主题、纸页颜色或按钮视觉样式。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-09" --workers=1`，1/1 PASS；断言空闲态“清空设置”可用、title 正确，60 分钟草稿可清除，运行态“重置本次专注”可用，重置后标题为空、时长恢复 45 分钟并显示反馈。
- 截图证据：修复前 `output/qa/REFINE-19/TIMER-09-before-9ecaf59.png`；修复后 `output/qa/REFINE-19/TIMER-09-after-active-9ecaf59.png`、`output/qa/REFINE-19/TIMER-09-after-clear-9ecaf59.png`。
- 最终状态：已修复。按钮在空闲/有进度两种状态下均可用且用途明确，清除范围与反馈一致。

#### TIMER-10：快捷键提示没有必要

原始问题：

> 页面下方快捷键提示没有存在必要，且实际快捷键体验不可靠。

检查编辑纸页是否有未接入或不可靠的快捷键说明；只移除无效提示，不删除真实全局能力。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 页面下方快捷键提示没有存在必要，且实际快捷键体验不可靠。
- 编辑纸页对应区域：`.ep-focus-footer` 及计时页底部是否存在快捷键提示；设置页 `.ep-settings-paper--shortcuts` 的快捷键说明；MainShell 全局键盘处理。
- 复现数据或状态：Chromium + Tauri mock；计时页没有 `kbd` 或 Ctrl 提示；设置页列出的三项快捷键为“开始 / 继续 Ctrl+Enter”“结束本段 Ctrl+Shift+E”“命令面板 Ctrl+K”。无待办空数据下分别触发 Ctrl+Enter、Ctrl+Shift+E、Ctrl+K，前两者给出对应业务提示，Ctrl+K 打开命令面板。
- 是否复现：否，PASS。编辑纸页计时页没有无效的底部快捷键提示；设置页说明的三项均有真实全局处理，不应删除。
- 根因：未发现回归；快捷键说明集中在设置页，且与 `MainShell.tsx` 的全局 keydown 分支一致。
- 实际修改：无；不移除真实快捷键，不增加计时页底部提示。
- 未修改范围：不修改全局快捷键处理、命令面板、计时操作、设置页快捷键说明、其他主题或纸页风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-10" --workers=1`，1/1 PASS；断言计时页无 Ctrl/kbd 底部提示、设置页有 7 个真实快捷键键帽，并验证 Ctrl+Enter、Ctrl+Shift+E 和 Ctrl+K 的实际响应。
- 截图证据：`output/qa/REFINE-19/TIMER-10-pass-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

#### TIMER-11：全屏右侧卡片排版

原始问题：

> 计时页放到全屏后，右侧卡片仍然出现文字排版问题。

在全屏和最大化下单独记录右侧卡片每个元素的 bounding box 和重叠关系。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 计时页放到全屏后，右侧卡片仍然出现文字排版问题。
- 编辑纸页对应区域：`.ep-focus-notes` 右侧备注区、`.ep-note-paper` 的标题/三个字段和 `.ep-mini-note`，以及其在中央计时卡旁的整体位置。
- 复现数据或状态：Chromium + Tauri mock；分别测量 `2560×1368`、Windows 150% 对应 CSS `1707×912` 和 `1487×1058`。在 CSS `1707×912` 下右侧区域为 `1224.25–1543.50px × 319.23–782.23px`，五个直接内容块均有正高度、无两两重叠，文档宽度等于视口宽度。
- 是否复现：否，PASS。全屏和最大化下右侧纸张每个元素均在视口内，标题、字段、关联区域和下一次休息提示没有排版重叠或横向越界。
- 根因：未发现编辑纸页同类根因；右侧备注区采用独立网格列和普通文档流，最大化时没有绝对定位文字。
- 实际修改：无；不为未复现的全屏排版问题改变右侧纸张尺寸、旋转或列布局。
- 未修改范围：不修改右侧专注输入、计时卡、全屏窗口行为、其他主题或 TIMER-08 长文本规则。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-11" --workers=1`，1/1 PASS；逐尺寸记录右侧容器与五个内容块 bounding box，断言无两两重叠、无横向溢出，桌面视口下底部不越界。
- 截图证据：`output/qa/REFINE-19/TIMER-11-pass-9ecaf59-1707x912.png`。
- 最终状态：PASS；不需要代码修复。

#### TIMER-12：01:00:00 压住统计

原始问题：

> 01:00:00 大计时读数压住下面的“目标时长 / 今日已完成”。

使用 60 分钟倒计时复现，并检查编辑纸页是否有相同重叠。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 01:00:00 大计时读数压住下面的“目标时长 / 今日已完成”。
- 编辑纸页对应区域：`.ep-clock-card__readout` 的 60 分钟读数、模式切换、`.ep-inline-field` 目标时长、`.ep-clock-card__actions`，以及左侧“今日专注”纸条。
- 复现数据或状态：Chromium + Tauri mock；将倒计时输入设为 60 分钟，读数显示 `01:00:00`；覆盖 `2560×1368`、CSS `1707×912` 和 `420×720`。CSS `1707×912` 下读数为 `823.53–1015.97 × 443.63–544.83px`，模式为 `774.75–1064.75 × 660.23–699.23px`，目标时长为 `831.27–1008.23 × 715.23–757.23px`，操作区为 `787.83–1051.67 × 773.23–813.23px`，均无相互覆盖；编辑纸页没有“今日已完成”同名字段，等价“今日专注”纸条在独立左列。
- 是否复现：不适用 + PASS。60 分钟长读数没有压住目标时长、操作区或今日专注统计；编辑纸页的统计与计时读数分列，不存在第一主题同类重叠。
- 根因：未发现编辑纸页同类根因；圆盘内部只承载状态/读数/说明，模式、目标时长和操作均通过独立网格间距排列。
- 实际修改：无；不因未复现的长读数问题改变纸页计时卡结构。
- 未修改范围：不修改读数格式、目标时长、今日专注统计口径、计时状态机、其他主题或 TIMER-02 尺寸修复。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-12" --workers=1`，1/1 PASS；覆盖三种视口，断言 `01:00:00` 在圆盘内、与模式/目标时长/操作/今日专注纸条无两两重叠且无横向溢出。
- 截图证据：`output/qa/REFINE-19/TIMER-12-pass-9ecaf59-1707x912.png`。
- 最终状态：不适用 + PASS；不需要代码修复。

#### TIMER-13：状态说明与模式说明互相覆盖

原始问题：

> 状态说明和“倒计时 · 设定 60 分钟”也被挤在相邻区域，卡片信息互相覆盖。

使用 45、60、90 分钟和长状态文案逐条检查。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 状态说明和“倒计时 · 设定 60 分钟”也被挤在相邻区域，卡片信息互相覆盖。
- 编辑纸页对应区域：`.ep-clock-card__readout` 内状态、时间读数、模式/目标说明，以及其下方模式切换、目标时长和操作区。
- 复现数据或状态：Chromium + Tauri mock；依次输入 45、60、90 分钟，读数分别为 `00:45:00`、`01:00:00`、`01:30:00`，同时检查状态“待开始”和“设定 N 分钟”。在 `1707×912` 下状态、读数、模式说明依次为 `443.63–460.63px`、`468.63–519.83px`、`527.83–544.83px`，模式切换从 `660.23px` 开始，目标时长从 `715.23px` 开始，操作从 `773.23px` 开始。
- 是否复现：否，PASS。45/60/90 分钟和窄屏下状态、长读数、模式说明、目标时长与操作区均按网格间距分开，没有覆盖或挤压；实际应用状态文案均为短状态，不存在长状态排版问题。
- 根因：未发现编辑纸页同类根因；状态、读数和说明采用中央圆盘内的 grid 行，圆盘与下方控件之间有固定间距。
- 实际修改：无；不为未复现的问题改变计时卡行高、字体或纸页结构。
- 未修改范围：不修改读数格式、状态来源、目标时长、模式切换、其他主题或 TIMER-02 圆盘尺寸。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-13" --workers=1`，1/1 PASS；覆盖 45/60/90 分钟、`2560×1368`、`1707×912`、`420×720`，断言状态/读数/说明/模式/目标时长/操作按序分离。
- 截图证据：`output/qa/REFINE-19/TIMER-13-pass-9ecaf59-1707x912.png`。
- 最终状态：PASS；不需要代码修复。

#### TIMER-14：Windows 高 DPI 视口

原始问题：

> 问题出现在物理 2560×1368 全屏截图中；Windows 显示缩放后，实际 CSS 视口约为 1707×912。

编辑纸页计时页必须单独覆盖物理尺寸、CSS 尺寸和 DPR 组合，不能只用普通 100% 视口。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 问题出现在物理 2560×1368 全屏截图中；Windows 显示缩放后，实际 CSS 视口约为 1707×912。
- 编辑纸页对应区域：带 `deviceScaleFactor` 的 Chromium `.ep-focus-page`、中央计时卡、右侧备注纸、页脚和文档滚动宽度。
- 复现数据或状态：建立三个独立上下文：物理代理 `2560×1368 / DPR 1`、Windows 150% CSS 代理 `1707×912 / DPR 1.5`、普通 `1487×1058 / DPR 1`。在 CSS `1707×912 / DPR 1.5` 下编辑纸页范围为 `361.50–1543.50 × 112–900.23px`，中央卡为 `633.25–1206.25 × 319.23–834.23px`，右侧备注为 `1224.25–1543.50 × 319.23–782.23px`，页脚 bottom `900.23px`。
- 是否复现：否，PASS。三种 CSS/DPR 组合下页面、计时卡、右侧备注和页脚均不越过 CSS 视口，文档无横向溢出；未出现第一主题高 DPI 截图中的裁切或错位。
- 根因：未发现编辑纸页同类根因；主题使用普通文档流布局，不依赖物理像素固定定位。
- 实际修改：无；不为未复现的高 DPI 问题改变 CSS 尺寸或纸页缩放。
- 未修改范围：不修改 Tauri 窗口尺寸、DPR、全屏行为、其他主题或已有计时布局。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-14" --workers=1`，1/1 PASS；三个独立上下文分别验证 viewport、DPR、文档宽度及四个关键区域边界。
- 截图证据：`output/qa/REFINE-19/TIMER-14-pass-9ecaf59-physical-2560x1368-dpr1.png`、`...-css-1707x912-dpr1.5.png`、`...-css-1487x1058-dpr1.png`。
- 最终状态：PASS；不需要代码修复。

#### TIMER-15：今日完成数不一致

历史问题：

> “今日”页右卡已经显示今天有 1 段专注，但切换到“计时”页后仍显示“今日已完成 0 段”。

注入当天已有记录、当前计时运行态完成数为 0 的夹具，确认编辑纸页读取正确的按日记录统计。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> “今日”页右卡已经显示今天有 1 段专注，但切换到“计时”页后仍显示“今日已完成 0 段”。
- 编辑纸页对应区域：`.ep-focus-page` 左侧“今日专注”纸条，以及 Today 页的完成段数统计。
- 复现数据或状态：Chromium + Tauri mock；注入当天 1 条专注记录、0 个已完成待办。修复前 Today 页完成段数为 `1`，切换计时页后左侧纸条仍显示 `0 项完成`；修复后同一夹具显示 `1 段完成`。
- 是否复现：是。复现了按待办完成数读取专注完成数的统计口径错误。
- 根因：编辑纸页 Focus 直接使用 `props.todos().filter(item => item.isCompleted).length`，而 Today 页使用后端按日专注记录统计 `todaySessionCount`，两个页面读取了不同事实源。
- 实际修改：在 `src/components/EditorialPaperViews.tsx` 将“今日专注”纸条改为读取 `props.todaySessionCount()`，并将单位文案明确为“段完成”。
- 未修改范围：不修改后端统计、Today 页口径、待办完成数、计时状态机、其他主题或纸页布局。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-15" --workers=1`，1/1 PASS；断言 Today 与 Focus 在 1 条当天记录、0 个已完成待办夹具下均显示 1。
- 截图证据：修复前 `output/qa/REFINE-19/TIMER-15-before-9ecaf59.png`；修复后 `output/qa/REFINE-19/TIMER-15-after-9ecaf59.png`。
- 最终状态：已修复；Today 与 Focus 现在使用同一按日专注记录事实源。

#### TIMER-16：返回主界面后没有悬浮窗入口

原始问题：

> 用户确认计时开始时自动进入专注悬浮窗没有问题，但从悬浮窗点击“返回”进入主窗口后，Night Valley“计时”页没有再次进入悬浮窗的按钮。

检查编辑纸页是否存在同类入口缺失。没有对应入口或主题行为时记录实际状态，不强行新增。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 用户确认计时开始时自动进入专注悬浮窗没有问题，但从悬浮窗点击“返回”进入主窗口后，Night Valley“计时”页没有再次进入悬浮窗的按钮。
- 编辑纸页对应区域：`.ep-focus-page` 页脚、已有 `onShowFocusFloating` 窗口回调，以及本轮运行/暂停态的入口可见性。
- 复现数据或状态：Chromium + Tauri mock；视口 `1487×1058`。填写专注主题并点击“开始专注”后状态为“倒计时中”。修复前截图中页脚没有“进入悬浮窗”按钮；修复后只有在 `timerHasProgress()` 为真时显示该按钮，按钮带 title“隐藏主窗口，回到悬浮计时”，点击后再次调用 `show_focus_floating`。
- 是否复现：是。编辑纸页复现了从悬浮窗返回后缺少重新进入入口的同类问题。
- 根因：`MainShell` 已将 `onShowFocusFloating` 传入编辑纸页，且开始动作会自动调用 `showFocusFloating()`；但 `EditorialPaperFocus` 没有渲染该回调对应的用户入口，只有 Night Valley 计时页有“进入悬浮窗”按钮。
- 实际修改：在 `src/components/EditorialPaperViews.tsx` 页脚增加条件化“进入悬浮窗”按钮；在 `src/components/EditorialPaperViews.css` 仅增加页脚内紧凑尺寸规则，避免活动态入口改变计时卡的纵向结构。
- 未修改范围：不修改开始后的自动悬浮、Tauri 原生窗口、返回行为、Night Valley、悬浮窗视觉样式或编辑纸页整体风格；空闲态不显示该入口。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-16" --workers=1`，1/1 PASS；断言运行态入口可见、title 正确，并验证点击会再次调用 `show_focus_floating`。
- 截图证据：修复前 `output/qa/REFINE-19/TIMER-16-before-9ecaf59.png`；修复后 `output/qa/REFINE-19/TIMER-16-after-9ecaf59.png`。
- 最终状态：已修复；返回主界面后，编辑纸页计时页首屏页脚可以重新打开悬浮计时。

#### TIMER-17：开始后悬浮窗打开

历史目标问题：

> 点击开始后悬浮窗会打开

检查编辑纸页开始动作后的窗口和状态行为。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 点击开始后悬浮窗会打开
- 编辑纸页对应区域：`.ep-focus-page` 的“开始专注”动作、计时状态和 `show_focus_floating` 窗口调用。
- 复现数据或状态：Chromium + Tauri mock；视口 `1487×1058`，填写主题“核对开始后的悬浮计时状态”并点击“开始专注”。页面进入“倒计时中”，并记录到恰好 1 次 `show_focus_floating` 调用。
- 是否复现：否，PASS。编辑纸页开始动作仍然触发自动悬浮窗打开，没有出现开始后停留主窗口的同类问题。
- 根因：未发现回归；`MainShell.startFocus()` 在计时上下文更新和 `start_timer` 成功后统一调用已有 `showFocusFloating()`，编辑纸页只负责触发同一开始回调。
- 实际修改：无；TIMER-16 只补充返回主界面后的再次进入入口，不改变开始时的自动悬浮路径。
- 未修改范围：不修改开始按钮、计时状态机、Tauri 窗口显示逻辑、Night Valley 或编辑纸页样式。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-17" --workers=1`，1/1 PASS；验证运行态并断言 `show_focus_floating` 恰好调用一次。
- 截图证据：`output/qa/REFINE-19/TIMER-17-pass-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

#### TIMER-18：主界面没有自动隐藏

历史目标问题：

> 但是主界面没有自动隐藏

检查编辑纸页对应行为是否出现同样不同步。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 但是主界面没有自动隐藏
- 编辑纸页对应区域：`.ep-focus-page` 的开始动作、`MainShell.startFocus()` 到 `showFocusFloating()` 的前端调用链，以及 Rust `show_focus_floating` 命令。
- 复现数据或状态：Chromium + Tauri mock；视口 `1487×1058`，填写主题并开始后页面进入“倒计时中”，前端记录到 1 次 `show_focus_floating`，没有额外的 `hide_main_window` 前端分支。浏览器夹具只模拟 invoke，不能直接观测真实原生窗口可见性；同步核对 `src-tauri/src/runtime.rs` 中该命令在显示并聚焦专注小窗后执行 `main_window.hide()`。
- 是否复现：否，PASS。编辑纸页没有出现与第一主题相同的“开始后主窗口不隐藏”调用链缺失；原生隐藏动作由同一命令实现。
- 根因：未发现编辑纸页回归；开始动作统一走 `MainShell.startFocus()`，而 `show_focus_floating` 的原生实现负责专注小窗显示、聚焦和主窗口隐藏。
- 实际修改：无；TIMER-16 的返回入口仍复用同一原生命令，不另造主题专属的隐藏逻辑。
- 未修改范围：不修改 Rust 窗口命令、主窗口生命周期、开始/暂停状态机、Night Valley 或编辑纸页布局。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-18" --workers=1`，1/1 PASS；验证开始后状态、`show_focus_floating` 恰好一次且前端没有错误的独立隐藏命令，并结合 Rust 实现核对原生隐藏动作。
- 截图证据：`output/qa/REFINE-19/TIMER-18-pass-9ecaf59.png`；该截图记录编辑纸页开始后的前端状态，原生隐藏证据为 `src-tauri/src/runtime.rs` 的命令实现。
- 最终状态：PASS；不需要代码修复。

#### TIMER-19：返回后无法重新打开悬浮窗

历史目标问题：

> 从悬浮窗返回主界面后，没有重新打开悬浮窗的按钮

检查入口是否存在、是否在首屏、是否可点击。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 从悬浮窗返回主界面后，没有重新打开悬浮窗的按钮
- 编辑纸页对应区域：`.ep-focus-page` 页脚的“进入悬浮窗”按钮，以及运行态、暂停态的首屏位置和点击行为。
- 复现数据或状态：Chromium + Tauri mock；视口 `1487×1058`。开始后入口位于首屏，bounding box 的 top 不小于 0、bottom 不超过 viewport；点击运行态入口后 `show_focus_floating` 调用次数增加 1，暂停后入口仍可见且再次点击还能增加 1 次调用。
- 是否复现：否，PASS。TIMER-16 修复后没有再现返回主界面缺少入口、入口被放到首屏以下或暂停态消失的问题。
- 根因：未发现当前实现的同类问题；入口绑定已有窗口回调，并按 `timerHasProgress()` 覆盖运行和暂停两种可继续状态。
- 实际修改：无；本项复用 TIMER-16 已增加的入口，不重复改变按钮文案、位置或悬浮窗逻辑。
- 未修改范围：不修改返回主界面动作、原生窗口调用、运行/暂停状态机、空闲态布局、Night Valley 或编辑纸页风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-19" --workers=1`，1/1 PASS；验证入口首屏边界、运行态点击和暂停态点击。
- 截图证据：`output/qa/REFINE-19/TIMER-19-pass-9ecaf59.png`。
- 最终状态：PASS；TIMER-16 的修复已覆盖本项，不需要再次修改代码。

#### TIMER-20：计时和界面状态不一致

历史目标问题：

> 计时状态和界面状态可能出现不一致。

分别检查运行、暂停、继续、完成、重置、切换页面和重启。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 计时状态和界面状态可能出现不一致。
- 编辑纸页对应区域：`.ep-focus-page` 左侧状态纸条、圆盘状态/读数、开始/暂停/继续/完成/重置按钮、专注主题表单、悬浮窗入口，以及切换页面和刷新后的恢复状态。
- 复现数据或状态：Chromium + Tauri mock；按运行→暂停→继续→切换“今日”再回到“计时”→完成并记录→再次开始→重置→刷新顺序执行。运行态均为“倒计时中”且按钮为“暂停”；暂停态为“已暂停”且按钮为“继续专注”；切页回来仍保留运行态和已锁定主题；完成后显示保存反馈、恢复“未开始”、清空主题并隐藏悬浮入口；重置后恢复干净状态；刷新后从后端初始快照显示“待开始”，表单为空。
- 是否复现：否，PASS。没有出现状态文本、圆盘读数、主按钮、输入锁定、悬浮入口或完成/重置结果互相不同步。刷新后的“待开始”是后端初始快照，与操作完成后的“未开始”是不同生命周期状态，不是显示冲突。
- 根因：未发现当前实现的同类根因；`MainShell.applyTimerSnapshot()` 统一更新计时快照、输入上下文和锁定条件，编辑纸页各区域读取同一组 props。TIMER-07、TIMER-09、TIMER-15 和 TIMER-16 的局部修复均沿用该单一快照链路。
- 实际修改：无；本项不重复修改状态机或快照同步逻辑。
- 未修改范围：不修改计时 API、完成记录保存、reset 语义、页面切换、刷新恢复、悬浮窗行为、其他主题或编辑纸页风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-20" --workers=1`，1/1 PASS；覆盖运行、暂停、继续、切页、完成、再次开始、重置和刷新恢复，并检查对应控件与表单状态。
- 截图证据：`output/qa/REFINE-19/TIMER-20-pass-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

#### TIMER-21：查看记录按钮文字不居中

历史问题：

> 截图中的“查看专注记录”按钮文字没有处于按钮正中。

检查编辑纸页对应按钮的文字、图标、箭头和整体几何中心，不能只看外框。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 截图中的“查看专注记录”按钮文字没有处于按钮正中。
- 编辑纸页对应区域：`.ep-focus-page` 计时页底部操作区域，以及 Today 页 `.ep-facts-row__actions` 的“回看记录”入口。
- 复现数据或状态：Chromium + Tauri mock；视口 `1487×1058`。计时页没有名为“查看专注记录”的按钮或同名文案；Today 页存在独立的“回看记录”文字操作，用于进入记录页，不是第一主题截图中的带边框“查看专注记录”按钮。
- 是否复现：不适用 + PASS。编辑纸页不存在该对应控件，因此没有可测量的文字/图标/箭头居中问题。
- 根因：编辑纸页将记录入口放在 Today 页事实栏的文字操作中，计时页不承担查看记录入口；不存在错误复用第一主题按钮结构的情况。
- 实际修改：无；不为不存在的控件新增按钮或改变编辑纸页信息架构。
- 未修改范围：不修改 Today 页“回看记录”、记录页导航、计时页页脚、按钮对齐规则、其他主题或纸页风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TIMER-21" --workers=1`，1/1 PASS；断言计时页无同名按钮和文案，同时确认 Today 页“回看记录”入口存在。
- 截图证据：`output/qa/REFINE-19/TIMER-21-not-applicable-9ecaf59.png`。
- 最终状态：不适用 + PASS；不需要代码修复。

### 2.4 待办页

#### TODO-01：中间文字重叠、空白过多、需要重构

原始问题：

> 回到待办这个界面，中间仍然有文字重叠的问题存在，然后又请你帮我重新构思一下这个界面，感觉有一些空，重构

拆成三项独立检查：

1. 中间列标题、说明、任务和按钮是否互相覆盖。
2. 空白是否由固定高度、绝对定位或内容没有撑开造成。
3. 页面是否能明确表达待开始、进行中和已完成事项。

只修复真实布局问题，保留编辑纸页工作簿风格，不改成 Night Valley 工作台。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 回到待办这个界面，中间仍然有文字重叠的问题存在，然后又请你帮我重新构思一下这个界面，感觉有一些空，重构
- 编辑纸页对应区域：`.ep-todo-book` 三列（今天、稍后、已完成）的标题/计数、空状态说明、长标题行及编辑/专注/删除操作；同时检查列列表高度和窄屏堆叠。
- 复现数据或状态：Chromium + Tauri mock；注入 3 条长标题待开始事项、2 条长标题已完成事项，覆盖 `1487×1058`、`1120×760`、`820×720`。修复前三列列表被统一固定为 `230px`，在 `820px` 堆叠视图中空的“稍后”列产生明显无内容留白；标题、说明和操作按钮的 bounding box 没有两两重叠，文档宽度等于视口宽度。修复后“今天/稍后/已完成”列表分别按内容约为 `195px/118px/130px`，空列不再保留固定大块空白，三种状态文案仍完整可见。
- 是否复现：是。复现了“空白过多”的同类布局问题；未复现“中间文字重叠”。
- 根因：`.ep-todo-column__list` 对所有列设置 `min-height: 230px`，且移动端将三列改为纵向堆叠，空状态列仍占用与多条任务列相同的最小高度；这不是编辑纸页风格所必需的结构。
- 实际修改：在 `src/components/EditorialPaperViews.css` 将 `.ep-todo-column__list` 的固定 `min-height` 改为 `0`，让列表由空状态或任务行自身内容撑开；保留三列工作簿结构、纸页边框、行间距、状态文案和所有操作。
- 未修改范围：不重构为 Night Valley 工作台、不修改待办数据口径、长标题省略规则、任务操作、窗口控件、其他主题或编辑纸页颜色/字体。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODO-01" --workers=1`，1/1 PASS；覆盖长标题、3 条待开始、空“稍后”、2 条已完成事项及 `1487×1058`、`1120×760`、`820×720`，断言文字/操作无重叠、空列列表小于原固定高度、三种状态清晰且无横向溢出。
- 截图证据：修复前 `output/qa/REFINE-19/TODO-01-before-9ecaf59.png`；修复后 `output/qa/REFINE-19/TODO-01-after-9ecaf59.png`。
- 最终状态：已修复；只收紧真实固定留白，不改变编辑纸页主题风格或信息结构。

#### TODO-02：已完成事项无法查看和操作

历史复述：

> 待办页中间文字仍有重叠，界面留白较多，且已完成事项数量增加后无法正常查看和操作；此前记录页修复后还出现过右上角三个原生窗口控件不可用，要求继续保持可检查。

检查长完成列表、长标题、恢复、删除、滚动、窄屏堆叠和横向溢出。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 待办页中间文字仍有重叠，界面留白较多，且已完成事项数量增加后无法正常查看和操作；此前记录页修复后还出现过右上角三个原生窗口控件不可用，要求继续保持可检查。
- 编辑纸页对应区域：`.ep-todo-column--done` 的长完成事项列表、每行的完成/恢复、编辑、专注、删除操作，以及桌面三列 spread 与 `820px/560px/420px` 窄屏堆叠。
- 复现数据或状态：Chromium + Tauri mock；注入 18 条长完成事项和 2 条待开始事项。桌面完成首项编辑、末项滚动到达并恢复、再删除一项，最终已完成列表保留 16 行；完成列表 `scrollHeight > 1000px`，未出现内部裁切。`1487×1058`、`820×720`、`560×720`、`420×720` 下文档横向宽度均不超过视口，行内勾选、文本和操作块无重叠。
- 是否复现：部分复现。已完成事项“无法查看和操作”未复现；长列表下桌面空列被最长列撑高的同类留白复现并修复。此前提到的中间文字重叠在本轮仍未复现。
- 根因：TODO-01 已移除 `.ep-todo-column__list` 的固定 `min-height: 230px`；本项进一步发现 `.ep-todo-book` 的默认 `align-items: stretch` 会让今天/稍后列跟随已完成长列表拉高，造成非内容区域留白。操作本身使用正常文档流和独立按钮，没有被长列表裁切。
- 实际修改：在 `src/components/EditorialPaperViews.css` 为 `.ep-todo-book` 增加 `align-items: start`，使每列按自身内容高度结束；结合 TODO-01 的 `min-height: 0`，保留长完成列表自然滚动和现有工作簿视觉。
- 未修改范围：不引入内部滚动容器、不改变完成事项排序、待办 API、编辑/恢复/删除语义、长标题 title 提示、窗口控件、其他主题或编辑纸页风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODO-02" --workers=1`，1/1 PASS；18 条完成事项覆盖滚动、编辑、恢复、删除，检查完成列表高度、三种窄屏堆叠、横向边界和行内块重叠。
- 截图证据：`output/qa/REFINE-19/TODO-02-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/TODO-02-pass-420-9ecaf59.png`。
- 最终状态：已修复并通过；完成事项可回看、可操作，长列表不会把其他列的内容布局拉伸到无意义空白。

#### TODO-03：窗口控件回归

原始要求：

> 此前记录页修复后还出现过右上角三个原生窗口控件不可用，要求继续保持可检查。

在待办页实际检查三个窗口控件，不只依赖浏览器 mock。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 此前记录页修复后还出现过右上角三个原生窗口控件不可用，要求继续保持可检查。
- 编辑纸页对应区域：待办页右上角 `.window-controls` 的最小化、最大化/还原、关闭三个按钮。
- 复现数据或状态：Chromium + Tauri mock；在待办页 `1487×1058` 与 `420×720` 下均找到 3 个按钮，三者都在视口边界内，分别具有 aria-label“最小化窗口”“最大化或还原窗口”“关闭窗口”和对应 title。安全夹具中逐个点击后分别分发 `minimize_main_window`、`toggle_maximize_main_window`、`close_main_window`；同时核对 `src/MainShell.tsx`、`src/lib/window-controls.ts` 与 `src-tauri/src/runtime.rs` 的真实命令映射。未操作或停止 PID `23304` 用户进程。
- 是否复现：否，PASS。待办页没有出现三个控件消失、不可见、越界或绑定错误的同类问题。
- 根因：未发现回归；编辑纸页主题边界只调整控件的静态布局与圆形外观，没有移除 MainShell 的事件绑定；关闭命令仍保持“隐藏到托盘”语义。
- 实际修改：无；不改窗口控件尺寸、命令实现、窗口生命周期或其他主题样式。
- 未修改范围：不修改最小化/最大化/关闭 Rust 命令、主窗口进程、待办页布局、悬浮窗行为或用户进程。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 TODO-03" --workers=1`，1/1 PASS；覆盖桌面与 `420px`，检查 3 个控件的可见性、边界、aria/title 和三条命令分发，并结合源码核对原生映射。
- 截图证据：`output/qa/REFINE-19/TODO-03-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/TODO-03-pass-420-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

### 2.5 记录页

#### RECORDS-01：首屏严重折叠

历史问题：

> 首屏严重折叠

检查标题、七日概览、统计摘要、选中日期详情和第一条记录是否在首屏可见。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 首屏严重折叠
- 编辑纸页对应区域：`.ep-records-header` 页眉、`.ep-archive-paper` 七日概览/统计摘要、`.ep-records-spread` 选中日期标题与记录列表第一条，以及页面横向边界。
- 复现数据或状态：Chromium + Tauri mock；注入 2026-08-30 至 2026-09-05 七日数据和 3 条当天记录。`1487×1058` 下页眉 bottom `297.23px`、七日概览 bottom `613.23px`、记录 spread bottom `903.23px`，第一条记录 `699.23–767.23px`，均在首屏；`1120×760` 下内容自然向下滚动，首条记录可滚动到达，文档横向宽度等于视口。
- 是否复现：否，PASS。编辑纸页默认桌面没有出现首屏严重折叠；关键摘要、日期标题和第一条记录均有明确层级，窄桌面只产生正常纵向滚动。
- 根因：未发现编辑纸页同类根因；页眉、概览、记录 spread 使用普通文档流，图表与摘要在独立纸张中，记录列表不采用绝对定位覆盖首屏内容。
- 实际修改：无；测试夹具仅增加可控七日 `dailyBreakdown` 输入，不修改产品数据或布局。
- 未修改范围：不修改页眉字号、七日图高度、统计口径、记录排序、滚动容器、其他主题或编辑纸页风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 RECORDS-01" --workers=1`，1/1 PASS；覆盖七日数据、3 条当天记录、`1487×1058` 和 `1120×760`，断言关键区域边界、第一条记录首屏位置及横向无溢出。
- 截图证据：`output/qa/REFINE-19/RECORDS-01-before-9ecaf59-1487.png`。
- 最终状态：PASS；不需要代码修复。

#### RECORDS-02：页面层级与主题不匹配

历史问题：

> 页面层级与主题不匹配

检查是否泄漏 Night Valley 背景、错误边框、错误面板层级或其他主题固定布局。只修复主题边界泄漏，不重新设计编辑纸页。

执行结果（2026-09-14，`9ecaf59`）：

- 第一主题原话：> 页面层级与主题不匹配
- 编辑纸页对应区域：记录页根节点、页眉、七日概览纸、记录 spread、洞察纸和全部记录区域的主题类、背景、定位方式与边界。
- 复现数据或状态：Chromium + Tauri mock；七日数据、3 条当天记录，覆盖 `1487×1058` 与 `420×720`。根节点为 `minimal-app--records` 且不含 `minimal-app--trail`；`.trail-page`、`.trail-map`、`.nv-page`、`.nv-focus-panel`、`.nv-records-archive`、`.nv-settings-layout` 均不存在。关键区域无 URL 背景、无绝对定位越界，七日纸的渐变为编辑纸页自有装饰。
- 是否复现：否，PASS。没有复现 Night Valley 背景、错误面板层级、错误边框或其他主题固定布局泄漏；两种视口横向宽度均受控。
- 根因：未发现编辑纸页主题边界回归；`ThemeSurface` 在记录页只渲染 `EditorialPaperRecords`，区域由 `EditorialPaperViews.css` 的纸张 token 和普通文档流承载。
- 实际修改：无；不删除编辑纸页自己的渐变、洞察纸背景或边框，不复制其他主题的背景。
- 未修改范围：不修改主题切换、记录数据、页面层级、洞察文案、Night Valley 主题或编辑纸页视觉风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 RECORDS-02" --workers=1`，1/1 PASS；覆盖桌面与 `420px`，断言主题根类、错误主题 DOM 不存在、关键区域无 URL 背景/绝对定位和横向无溢出。
- 截图证据：`output/qa/REFINE-19/RECORDS-02-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/RECORDS-02-pass-420-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

#### RECORDS-03：历史过长难以回看

历史问题：

> 历史天数增加后内容过长且难以回看

注入 28 天及更长历史，检查独立滚动、日期分组展开收起、最旧日期可达、编辑删除和长文本可见。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 历史天数增加后内容过长且难以回看
- 编辑纸页对应区域：`.ep-records-list` 当前日期记录、`.ep-full-history` 日期分组、长记录标题、编辑/保存/删除操作和页面纵向滚动区域。
- 复现数据或状态：Chromium + Tauri mock；注入 2026-08-09 至 2026-09-05 的 28 个日期分组，并在 2026-09-05 放入 2 条当天记录；视口覆盖 `1487×1058` 与 `420×720`。日期分组可展开、收起，最旧日期可定位；页面文档高度超过桌面视口但横向宽度不越界；编辑保存、删除和删除反馈均可用。首轮长标题断言发现当前记录列表的省略标题没有 `title` 属性，完整标题无法从该行回看；展开后的历史详情可以显示完整长标题。
- 是否复现：部分复现。历史过长导致的不可滚动、分组不可用、最旧记录不可达和编辑/删除失效均未复现；长标题在当前记录列表中被省略且缺少完整文本辅助入口这一同类可回看缺口复现。
- 根因：`.ep-record-entry` 的非编辑态使用带省略样式的 `<strong>` 渲染 `record.title`，但没有将完整标题暴露为 `title` 属性；历史分组本身使用普通文档流和可展开 `<details>`，不是数据丢失或固定层遮挡。
- 实际修改：在 `src/components/EditorialPaperViews.tsx` 的当前记录标题 `<strong>` 增加 `title={record.title}`；保留原有单行省略、纸页间距、日期分组和滚动结构，不改变视觉样式。
- 未修改范围：不新增独立滚动容器，不改变历史排序、默认显示数量、展开状态规则、编辑/删除后端命令、记录数据、其他主题或编辑纸页整体风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 RECORDS-03" --workers=1`，1/1 PASS；断言 28 个日期分组、展开/收起、最旧日期可达、长标题、编辑保存、删除反馈、页面纵向滚动和 `420px` 横向边界，并检查 `update_focus_record_title`、`delete_focus_record` 命令。
- 截图证据：`output/qa/REFINE-19/RECORDS-03-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/RECORDS-03-pass-420-9ecaf59.png`。
- 最终状态：已修复部分复现项；历史记录现可按日期回看，长标题在当前记录行保留完整 `title` 入口，未复现部分不改动。

#### RECORDS-04：本周专注总览点线错位

原始问题：

> 首先是本周专注总览这里，你看图片中的点和线没有完全对的上，看着会非常难受

逐项检查：

- 日期点是否位于对应日期位置。
- 路径或连接线是否经过对应点。
- SVG 和 HTML 是否使用同一坐标系。
- 首点、末点和日期标签是否一致。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 首先是本周专注总览这里，你看图片中的点和线没有完全对的上，看着会非常难受
- 编辑纸页对应区域：`.ep-archive-chart` 的“近七日 / NATURAL DAYS”图表、七个 `.ep-chart-bar` 日期柱、时长标签、柱体基线和日期标签。
- 复现数据或状态：Chromium + Tauri mock；注入 2026-08-30 至 2026-09-05 的 7 天数据，覆盖 `1487×1058` 和 `420×720`。编辑纸页图表没有 SVG、path、circle 或 polyline 点线结构，而是七个带日期和真实时长的 HTML 柱形按钮；每个柱的中心与所属日期按钮中心偏差小于 1px，柱体基线位于日期标签上方，七个时长与七个日期一一对应，窄屏文档无横向溢出。
- 是否复现：不适用 + PASS。第一主题的“点与连接线错位”结构不存在；编辑纸页自身柱形图的日期、柱体和时长标签对齐正确。
- 根因：未发现编辑纸页同类根因；编辑纸页没有复用第一主题的 SVG 点线坐标系，图表由同一个 HTML grid 单元承载日期、柱体和数值。
- 实际修改：无；不把现有柱形图改成点线图，不调整日期间距、柱高、图表数据或编辑纸页视觉风格。
- 未修改范围：不修改统计口径、七日数据来源、SVG/Canvas 结构、Night Valley 记录图、其他主题或窄屏滚动规则。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 RECORDS-04" --workers=1`，1/1 PASS；覆盖桌面与 `420px`，断言无点线 SVG 结构、7 个真实日期柱的数值映射、柱/标签中心与基线关系以及横向边界。
- 截图证据：`output/qa/REFINE-19/RECORDS-04-not-applicable-1487-9ecaf59.png`、`output/qa/REFINE-19/RECORDS-04-not-applicable-420-9ecaf59.png`。
- 最终状态：不适用 + PASS；不需要代码修复。

#### RECORDS-05：分布图模糊且重复

原始问题：

> 分布这个图，表述的非常模糊，并且和上面的大图表达的意思好像差不多，这一块可以统计别的数据，重复表达没有意义

逐项检查：

- 标题是否说明统计口径。
- 是否重复表达七日总览。
- 柱形是否对应真实记录。
- 空数据是否容易理解。
- 是否需要改成另一种真实统计维度。

只有确认重复或模糊才改变统计表达；不为视觉效果制造数据。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 分布这个图，表述的非常模糊，并且和上面的大图表达的意思好像差不多，这一块可以统计别的数据，重复表达没有意义
- 编辑纸页对应区域：`.ep-archive-paper` 内的 `.ep-archive-chart`、`近七日 / NATURAL DAYS` 标题、七个日期柱、时长标签，以及下方 `.ep-full-history` 记录索引。
- 复现数据或状态：Chromium + Tauri mock；注入 7 天不等时长数据，覆盖 `1487×1058` 与 `420×720`。编辑纸页只渲染 1 个七日图表和 1 组七个自然日柱，图表标题明确为 `近七日 / NATURAL DAYS`，柱上数值与日期按输入顺序对应；没有第二个 distribution 图、重复图表或含糊的无口径图形，下方全部记录是文字索引而非重复图表，窄屏无横向溢出。
- 是否复现：不适用 + PASS。第一主题所说的“上面大图 + 分布图”重复结构在编辑纸页不存在；现有单一七日图表的统计口径和数据映射清楚。
- 根因：未发现编辑纸页同类根因；编辑纸页的图表结构从一开始就只有一个明确标注的自然日总览，没有额外复制同一数据的分布组件。
- 实际修改：无；不制造新的统计维度，不删除现有真实七日总览，不改图表标题、柱高算法、历史文字索引或纸页布局。
- 未修改范围：不修改数据源、统计口径、记录排序、图表颜色与柱形视觉、Night Valley 分布图、其他主题或编辑纸页整体风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 RECORDS-05" --workers=1`，1/1 PASS；覆盖桌面与 `420px`，断言只有一个图表/柱组、无 distribution 候选结构、标题口径明确、7 个时长与日期正确映射及横向边界。
- 截图证据：`output/qa/REFINE-19/RECORDS-05-not-applicable-1487-9ecaf59.png`、`output/qa/REFINE-19/RECORDS-05-not-applicable-420-9ecaf59.png`。
- 最终状态：不适用 + PASS；不需要代码修复。

#### RECORDS-06：全部记录无法正常使用

原始问题：

> 全部记录看不了了，没法正常使用

逐项检查：

- 是否出现空白条代替真实记录。
- 是否因为默认折叠让用户误以为没有数据。
- 是否能加载更多和滚动到最旧记录。
- 是否能编辑、删除、保存。
- 长历史下记录文字是否可见。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 全部记录看不了了，没法正常使用
- 编辑纸页对应区域：`.ep-full-history` 的全部记录日期分组与文字索引、`.ep-records-list` 当前日期记录、`继续展开记录` 分页入口、编辑保存/删除反馈和窄屏边界。
- 复现数据或状态：Chromium + Tauri mock；注入同一日期的 205 条长标题记录，覆盖 `1487×1058` 与 `420×720`。初始当前记录显示 200 条并出现 `继续展开记录 · 已显示 200 / 205`，点击后显示 205 条；全部记录分组显示 `205 轮`，展开后 205 条历史文字可见。编辑第一条并保存后标题更新，删除最后一条后反馈“已删除”且分组更新为 `204 轮`；展开后的全部历史显示 204 条，桌面和窄屏文档均无横向溢出。
- 是否复现：否，PASS。没有复现空白条替代真实记录、默认折叠导致数据丢失、无法继续加载、最旧记录不可达、编辑/删除/保存失效或长文本不可见的问题。
- 根因：未发现编辑纸页同类根因；全部记录使用日期分组 `<details>`，当前日期记录使用 200 条分段加载，数据与操作状态通过同一 `records()` 刷新，长文本在当前记录行有 `title` 且在展开历史中完整显示。
- 实际修改：无；RECORDS-03 的长标题 `title` 修复已被本项夹具复用验证，不额外改变全部记录分页阈值、排序、折叠逻辑或纸页布局。
- 未修改范围：不取消 200 条分段加载，不自动展开 205 条记录，不改后端记录读写、编辑删除命令、历史文本密度、其他主题或编辑纸页视觉风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 RECORDS-06" --workers=1`，1/1 PASS；断言 205→200/205→205 的加载链路、日期分组、展开后的 204 条历史文本、长标题、编辑保存、删除反馈、纵向滚动和 `420px` 横向边界。
- 截图证据：`output/qa/REFINE-19/RECORDS-06-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/RECORDS-06-pass-420-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

#### RECORDS-07：记录页窗口控件不可用

原始问题：

> 你现在修完右上角三个按钮直接用不了了，我连检查都检查不了了

在编辑纸页记录页单独验证最小化、最大化/还原和关闭，并检查与顶栏拖拽的冲突。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 你现在修完右上角三个按钮直接用不了了，我连检查都检查不了了
- 编辑纸页对应区域：记录页右上角 `.window-controls` 及其中的最小化、最大化/还原、关闭三个 `.window-control`，并核对其与主窗口拖拽处理的边界。
- 复现数据或状态：Chromium + Tauri mock；记录页在 `1487×1058` 与 `420×720` 下均显示 3 个按钮，按钮均在视口内，aria-label/title 分别为“最小化窗口/最小化”“最大化或还原窗口/最大化 / 还原”“关闭窗口/关闭窗口（隐藏到托盘）”。安全夹具中逐个点击后分别收到 `minimize_main_window`、`toggle_maximize_main_window`、`close_main_window`；`.window-controls` 没有 `data-tauri-drag-region`，源码 `handleMainWindowMouseDown` 也明确跳过 `.window-controls`。
- 是否复现：否，PASS。记录页三个控件均可见、可点击、命令映射正确，没有被顶栏拖拽吞掉或越出窄屏边界。
- 根因：未发现编辑纸页同类根因；窗口控件位于全局 `app-bar`，事件处理在 `MainShell` 中保留，并通过控件自身 `onMouseDown` 阻止拖拽冒泡。
- 实际修改：无；不改窗口控件大小、图标、原生命令、顶栏拖拽、关闭到托盘语义或编辑纸页视觉风格。
- 未修改范围：不操作或停止用户 PID `23304` 及其 WebView2 子进程，不运行 macOS workflow，不修改其他主题窗口控件或 Tauri 窗口生命周期。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 RECORDS-07" --workers=1`，1/1 PASS；覆盖桌面与 `420px`，检查 3 个控件可见性、aria/title、视口边界、非拖拽属性和三条命令分发，并结合 `src/MainShell.tsx` 与 `src/lib/window-controls.ts` 核对映射。
- 截图证据：`output/qa/REFINE-19/RECORDS-07-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/RECORDS-07-pass-420-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

### 2.6 设置页

#### SETTINGS-01：文字重叠

原始问题：

> 你仔细查看，仍然存在有不少的文字重叠问题

逐项检查左侧导航、标题、说明、开关、音效、主题预览、备份、底部提示和反馈文案。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 你仔细查看，仍然存在有不少的文字重叠问题
- 编辑纸页对应区域：设置页页眉、五个 `.ep-settings-paper` 纸张、主题色板、亮度/动效滑块、密度选择、行为开关、音效选择与按钮、快捷键行、数据备份区、底部保存提示及反馈文字。
- 复现数据或状态：Chromium + Tauri mock；覆盖 `1487×1058` 与 `420×720`。逐个测量每张纸内的标题、色板、滑块、密度、预览、行为列表、音效、快捷键、备份操作等相邻布局块；两种视口的同纸张块均无重叠，所有色板和底部提示均有正高度且在横向视口内，文档宽度分别为 `1487` 与 `420`。截图中桌面五个设置区按两列/整行排列，窄屏按单列自然堆叠。
- 是否复现：否，PASS。没有复现设置页文字重叠、控件压住说明、主题预览或底部提示越界的问题。
- 根因：未发现编辑纸页同类根因；设置纸张采用普通 grid/flex 文档流，行为说明、快捷键和备份文案各自有明确行高与间距，窄屏通过单列布局堆叠。
- 实际修改：无；不调整设置纸张尺寸、字体、行高、色板、文案或页脚布局。
- 未修改范围：不修改亮度/动效/密度交互、音效、快捷键、备份、反馈提示、其他主题或编辑纸页整体视觉风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 SETTINGS-01" --workers=1`，1/1 PASS；覆盖 `1487×1058` 与 `420×720`，测量设置纸张内部布局块、色板和页脚子元素，断言无重叠、正高度和横向边界。
- 截图证据：`output/qa/REFINE-19/SETTINGS-01-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/SETTINGS-01-pass-420-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

#### SETTINGS-02：两个滑动条和紧凑舒展作用不明显

原始问题：

> 你下面两个滑动条以及紧凑舒展我不知道有什么用，能看到变化，但也仅限于一点点，可以先去掉了

逐项检查：

- 控件是否真正影响编辑纸页。
- 变化是否足够让用户理解。
- 是否有保存、恢复和可解释的产品价值。

只在编辑纸页复现同类低价值问题时移除；否则保留并记录原因。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 你下面两个滑动条以及紧凑舒展我不知道有什么用，能看到变化，但也仅限于一点点，可以先去掉了
- 编辑纸页对应区域：设置页 `01 / APPEARANCE` 中的“纸面亮度”“动效程度”两个 range 控件和“界面密度 / 宽松 / 紧凑”选择。
- 复现数据或状态：源码与运行时核对确认两个滑块只更新 `MainShell` 的共享状态和本地存储变量；`--nv-visual-intensity` 在 CSS 中没有消费者，`data-density` 的实际紧凑规则只针对 Night Valley 选择器，编辑纸页没有对应规则，动效分级也只命中 Night Valley 的动画选择器。修复前 `SETTINGS-01` 截图可见三组控件，但它们对编辑纸页没有可解释的布局/外观价值。
- 是否复现：是。低价值/作用不明显同类问题复现：编辑纸页展示了对本主题不生效的两个滑块和密度切换。
- 根因：编辑纸页复用了跨主题 `NightValleySettingsProps` 和共享外观状态，但 `EditorialPaperViews.css` 没有消费这些变量/密度选择器；因此设置面板把其他主题的控制暴露到了不支持它们的主题。
- 实际修改：在 `src/components/EditorialPaperViews.tsx` 的编辑纸页外观纸张中移除两个 range 行和密度选择行；保留主题色板、当前样式预览以及底层共享状态/其他主题设置，不改变纸张颜色、字体、布局比例或其他主题行为。
- 未修改范围：不删除 `MainShell` 的视觉强度、动效强度、密度状态及其本地存储，不修改 Night Valley 设置，不新增伪造的视觉效果，不改变编辑纸页视觉风格或设置页其他功能。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 SETTINGS-02" --workers=1`，1/1 PASS；断言编辑纸页不再显示 `.ep-slider-row`/`.ep-density-row`，主题色板和实时预览保留，根状态与横向边界正常。
- 截图证据：修复前 `output/qa/REFINE-19/SETTINGS-02-before-1487-9ecaf59.png`、`output/qa/REFINE-19/SETTINGS-02-before-420-9ecaf59.png`；修复后 `output/qa/REFINE-19/SETTINGS-02-after-1487-9ecaf59.png`、`output/qa/REFINE-19/SETTINGS-02-after-420-9ecaf59.png`。
- 最终状态：已修复；编辑纸页不再展示对本主题无效的低价值外观控件，其他主题的共享设置能力保留。

#### SETTINGS-03：音效选项太少

原始问题：

> 音效这个可以多搞几个默认的，或者比较有名的，流行的提示音：

逐项检查默认音效数量、名称、试听、选择保存、恢复、自定义导入和删除。若当前已经满足，则不扩展。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 音效这个可以多搞几个默认的，或者比较有名的，流行的提示音：
- 编辑纸页对应区域：设置页 `03 / SOUND` 的提醒音效选择框、试听、导入音效、自定义音效启用和移除操作。
- 复现数据或状态：Chromium + Tauri mock；默认选择框包含 7 个内置选项（柔和铃音、明亮三连、沉稳脉冲、木鱼单击、玻璃回响、晨光和弦、老牧师原声）和 1 个初始禁用的自定义选项。选择“明亮三连”会调用 `update_timer_preferences` 并立即更新；试听通过合成音频上下文实际创建振荡器；导入合法 WAV 后自定义选项启用并自动选中，移除后恢复柔和铃音。桌面/窄屏音效区均无横向越界。
- 是否复现：否，PASS。编辑纸页已提供足够的 7 个默认音效，并且试听、选择保存、自定义导入和删除链路可用，不需要继续扩展“流行音效”数量。
- 根因：未发现编辑纸页同类根因；音效选项与共享 `TimerPreferences`、合成试听和自定义音频本地存储链路已接通。
- 实际修改：无；不新增音效、不替换现有音色、不生成外部音频资源。
- 未修改范围：不修改音效名称、提醒开关、音频播放实现、自定义音效大小/类型校验、其他主题或编辑纸页视觉风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 SETTINGS-03" --workers=1`，1/1 PASS；断言 7 个内置选项、初始自定义禁用、选择保存、试听振荡器、导入启用、移除恢复、3 次偏好更新和 `420px` 横向边界。
- 截图证据：`output/qa/REFINE-19/SETTINGS-03-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/SETTINGS-03-pass-420-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

#### SETTINGS-04：快捷键没有用

原始问题：

> 快捷键没啥用，可以去掉

检查编辑纸页是否展示无行为、未接入或不可靠的快捷键说明。只删除无效说明，不删除真实全局能力。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 快捷键没啥用，可以去掉
- 编辑纸页对应区域：设置页 `04 / SHORTCUTS` 的“开始 / 继续 Ctrl + Enter”“结束本段 Ctrl + Shift + E”“命令面板 Ctrl + K”三行说明，以及 `MainShell` 全局 `keydown` 处理。
- 复现数据或状态：Chromium + Tauri mock；设置页显示 3 条快捷键。按 `Control+K` 打开并按 `Escape` 关闭命令面板；按 `Control+Enter` 调用 `start_timer` 并进入有进度状态；按 `Control+Shift+E` 调用 `complete_focus_session` 并显示“已保存为一条专注记录”。安全夹具同时验证计时快照和完成返回数据链路，桌面/窄屏设置页无横向溢出。
- 是否复现：否，PASS。三条说明均对应真实全局行为，未发现“展示但没有行为”或快捷键失效，因此不删除。
- 根因：未发现编辑纸页同类根因；快捷键由 `MainShell` 的全局 `keydown` 监听处理，命令面板、开始/继续和结束动作均有真实回调。
- 实际修改：无；不删除真实快捷键说明，不替换快捷键，不改变命令面板、计时状态机或保存流程。
- 未修改范围：不修改 `Control`/`Meta` 兼容逻辑、开始/完成命令、快捷键文案、其他主题或编辑纸页视觉风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 SETTINGS-04" --workers=1`，1/1 PASS；断言三行说明、命令面板开关、开始计时命令、完成记录命令、反馈文案和 `420px` 横向边界，并结合 `src/MainShell.tsx` 核对全局键盘处理。
- 截图证据：`output/qa/REFINE-19/SETTINGS-04-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/SETTINGS-04-pass-420-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

#### SETTINGS-05：右侧 Night Valley 主题预览图丑且用途不清

原始问题：

> 右侧这个夜谷主题，这个图片太丑了，并且你这一块留着是要干什么呢？能不能换成别的有趣的界面呢

编辑纸页只检查同类问题：

- 右侧预览是否误用其他主题。
- 预览是否有明确用途和信息价值。
- 是否出现图片与编辑纸页风格不一致。

不替换编辑纸页主题，不复制 Night Valley 主题观测站，不生成新图片。仅当同类可用性问题真实复现时，在原纸页风格内处理。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 右侧这个夜谷主题，这个图片太丑了，并且你这一块留着是要干什么呢？能不能换成别的有趣的界面呢
- 编辑纸页对应区域：设置页 `01 / APPEARANCE` 的主题色板和 `.ep-live-preview` 当前样式预览。
- 复现数据或状态：Chromium + Tauri mock；编辑纸页设置中有 5 个主题色板和 1 个“当前样式预览”，预览显示“编辑纸页”及其描述“纸张、铅字与可读性的工作界面。”；预览没有图片、URL 背景或 Night Valley `.nv-*` 节点，覆盖 `1487×1058` 与 `420×720` 均无横向溢出。
- 是否复现：否，PASS。编辑纸页没有误用 Night Valley 右侧主题观测图，也没有用途不明的图片；当前预览明确说明正在选择的主题。
- 根因：未发现编辑纸页同类根因；Editorial Paper 使用自己的纸面色板和文字预览，Night Valley 主题观测站只存在于其他主题的设置实现中。
- 实际修改：无；不替换图片、不新增图片、不复制 Night Valley 观测站、不改变编辑纸页预览内容或主题风格。
- 未修改范围：不修改主题切换、色板、预览描述、Night Valley 主题、资源文件或其他主题设置布局。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 SETTINGS-05" --workers=1`，1/1 PASS；覆盖桌面与 `420px`，断言 5 个主题色板、明确预览文字、无 Night Valley 节点/图片/URL 背景及横向边界。
- 截图证据：`output/qa/REFINE-19/SETTINGS-05-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/SETTINGS-05-pass-420-9ecaf59.png`。
- 最终状态：PASS；不需要代码修复。

#### SETTINGS-06：保存设置语义不清

原始问题：

> 这个保存设置指的是？不应该改完默认保存吗？

逐项检查主题切换、提醒开关、音效选择和重启恢复：

- 修改后是否立即持久化。
- 写入失败是否明确提示。
- 重启后是否恢复。
- 是否仍有让用户误以为必须手动保存的文案或按钮。

执行结果（2026-09-14，工作树基于 `9ecaf59`）：

- 第一主题原话：> 这个保存设置指的是？不应该改完默认保存吗？
- 编辑纸页对应区域：设置页主题色板、提醒开关、音效选择、底部“保存外观设置”按钮和“更改将在下次打开应用时生效。”说明，以及偏好写入失败后的控件状态。
- 复现数据或状态：修复前编辑纸页的主题/提醒/音效操作已经即时写入或调用保存，但页脚仍显示手动“保存外观设置”和“下次打开应用时生效”，形成相互矛盾的语义；模拟偏好写入失败时，错误提示出现但音效 `<select>` 停留在未保存的新值。修复后主题色板即时写入并提示自动保留，提醒开关和音效选择即时保存，失败时控件回滚到最后成功值；重载夹具能恢复已保存的 `bright_bell` 和关闭的提醒开关。桌面/窄屏无横向溢出。
- 是否复现：是。保存语义不清和写入失败后控件显示未保存值两个同类问题均复现。
- 根因：编辑纸页沿用了跨主题外观保存按钮和过时的“下次打开生效”文案，但 `MainShell.selectTheme`、`updateVisualIntensity`/`updateDensity` 与 `saveTimerPreferences` 实际采用即时保存；通用 `saveTimerPreferences` 的错误路径只显示错误，没有用最后成功的 signal 值触发受控表单回滚。
- 实际修改：在 `src/components/EditorialPaperViews.tsx` 移除编辑纸页冗余“保存外观设置”按钮，将页脚说明改为“主题与提醒设置会立即保存。”；在 `src/MainShell.tsx` 的 `saveTimerPreferences` 中记录成功状态，失败后用当前成功偏好创建新 signal 值，强制受控 checkbox/select 回到未写入前的值。保留其他主题的外观保存回调和所有底层持久化能力。
- 未修改范围：不改变主题切换、提醒/音效数据格式、Rust 持久化、其他主题设置按钮、不新增自动保存机制，不改变编辑纸页颜色、字体、纸张布局或整体视觉风格。
- 定向测试：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19 SETTINGS-06" --workers=1`，1/1 PASS；验证无冗余手动保存按钮、即时主题保存、提醒开关/音效保存、模拟失败提示与控件回滚，以及重载后的偏好恢复和 `420px` 横向边界。
- 截图证据：修复前 `output/qa/REFINE-19/SETTINGS-06-before-1487-9ecaf59.png`、`output/qa/REFINE-19/SETTINGS-06-before-420-9ecaf59.png`；修复后 `output/qa/REFINE-19/SETTINGS-06-pass-1487-9ecaf59.png`、`output/qa/REFINE-19/SETTINGS-06-pass-420-9ecaf59.png`。
- 最终状态：已修复；编辑纸页明确采用即时保存，失败时不会继续显示未持久化的新值。

## 3. 执行阶段

### 阶段 0：启动与基线

- 按项目要求依次读取目标资料、AGENTS.md、PROJECT_PLAN.md、上下文摘要。
- 读取 THEME_REFINEMENT_PROMPT.md、PRODUCT.md 和本计划。
- 核对 git status、分支、HEAD、origin/main、当前版本、v2.10.18 Release 和 Windows Checks。
- 检查正在运行的 Focused Moment、Vite、Playwright、Tauri 进程；不停止用户进程。
- 只读确认 Editorial Paper 五页入口和当前测试基线。

### 阶段 1：今日

- 按 TODAY-01 至 TODAY-11 顺序逐条复现。
- 发现问题后先写反馈理解、根因和最小改动范围。
- 集中修改 EditorialPaperToday 及其主题 CSS。
- 跑今日定向测试和四档压力宽度截图。
- 填写每条结果后才能进入计时页。

### 阶段 2：计时

- 按 TIMER-01 至 TIMER-21 顺序逐条复现。
- 覆盖未开始、运行、暂停、完成、重置、45/60/90 分钟和悬浮窗相关状态。
- 集中修改 EditorialPaperFocus 及必要的共享时间/窗口边界。
- 跑计时定向测试、高 DPI 和全屏压力测试。

### 阶段 3：待办

- 按 TODO-01 至 TODO-03 顺序逐条复现。
- 使用长标题、多条完成事项、空状态和窄屏。
- 集中修改 EditorialPaperTodos 及其主题 CSS。
- 跑新增/编辑/完成/恢复/删除/滚动和窗口控件测试。

### 阶段 4：记录

- 按 RECORDS-01 至 RECORDS-07 顺序逐条复现。
- 使用七日数据、空数据、28 天历史、最旧日期和编辑删除动作。
- 集中修改 EditorialPaperRecords 及其图表/列表 CSS。
- 跑点线坐标、分布口径、全部记录和窗口控件测试。

### 阶段 5：设置

- 按 SETTINGS-01 至 SETTINGS-06 顺序逐条复现。
- 使用长文案、音效选择、试听、导入、删除、保存失败和重启恢复。
- 集中修改 EditorialPaperSettings 及其设置 CSS。
- 跑设置交互、持久化和响应式测试。

## 4. 每页验收矩阵

每页至少覆盖：

- 1487×1058 默认桌面；
- 1120×760 最小桌面；
- 820×720 窄桌面；
- 560×720 窄窗口；
- 420×720 极窄窗口；
- 2560×1368 物理全屏代理；
- Windows 高 DPI 对应的 CSS 视口；
- 长文本、空数据和真实数据。

页面动作必须按真实链路验证：

- 今日：进入今日、查看记录、进入计时、完成/跳过待办。
- 计时：输入事项、开始、暂停、继续、完成、重置、保存记录。
- 待办：新增、编辑、完成、恢复、删除、进入专注。
- 记录：选择日期、展开日期、加载更多、编辑、删除、保存。
- 设置：切换主题、提醒开关、音效选择/试听/导入/删除、备份和失败反馈。

## 5. 收口、版本和发布

- 每页内部的问题先集中修复和定向验证，不为单个问题单独 commit、push 或 release。
- 五页全部完成后，增加或更新逐条 QA 证据文件，填写每个编号的最终状态。
- 跑一次完整前端回归、pnpm check、Vite build、Rust fmt/check/test。
- 用户可见代码改动时 bump patch 版本，同步 package.json、Cargo.toml、Cargo.lock、tauri.conf.json、runtime.rs、README、前端版本文本、docs/vX.Y.Z/RELEASE_NOTES.md 和 PROJECT_PLAN.md。
- 只生成 Windows portable、Setup/NSIS、MSI；记录本地 SHA-256。
- 正常提交、推送 main、创建不可移动 tag、等待并核对 Windows Checks、更新 GitHub Release 和远端 digest。
- 不运行任何 macOS workflow。
- 发布前先把本地验证结果和截图交给用户做用户视角复核；未完成发布闭环前只能称为本地候选。

## 6. 每条问题的结果模板

~~~text
编号：
第一主题原话：
编辑纸页对应区域：
复现数据或状态：
是否复现：
根因：
实际修改：
未修改范围：
定向测试：
截图证据：
最终状态：
~~~

最终报告必须逐条列出所有编号，不能只写“整体通过”。

## 7. 本地整体验收记录（2026-09-14）

- 逐条执行顺序已完成：TODAY-01～11 → TIMER-01～21 → TODO-01～03 → RECORDS-01～07 → SETTINGS-01～06；每个编号均已填写原话、对应区域、复现数据或状态、复现结论、根因、修改/未修改范围、定向测试、截图证据和最终状态。
- 完整定向回归：`pnpm exec playwright test tests/today-visual.spec.mjs --grep "REFINE-19" --workers=1`，48/48 PASS；升版到 2.10.19 后再次运行仍为 48/48 PASS。
- 静态与构建验证：`pnpm check` PASS；`pnpm build` PASS（2066 modules）；`git diff --check` PASS；`cargo fmt --check --manifest-path src-tauri/Cargo.toml` PASS；`cargo check --locked --manifest-path src-tauri/Cargo.toml` PASS；`cargo test --locked --manifest-path src-tauri/Cargo.toml` 35/35 PASS。
- Windows 打包：`pnpm package:release` PASS；portable、Setup/NSIS、MSI 的版本元数据均为 2.10.19。本地 SHA-256 已写入 `docs/v2.10.19/RELEASE_NOTES.md`。
- 版本同步：`package.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json`、`src-tauri/src/runtime.rs` 和 `src/App.css` 已同步到 2.10.19；旧 v2.10.18 Release/tag/资产未修改。
- 保护边界：未运行或修改任何 macOS workflow；未停止用户进程。PID `23304` 仍运行旧的 `Focused Moment v2.10.18.exe`，因此导出脚本保留旧文件并给出占用警告。
- 当前状态：本地候选完成，等待 REL-22 完成提交、推送、Windows Checks、tag/Release 和远端资产 digest 核对。
