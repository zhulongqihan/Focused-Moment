# CORE-03 macOS 原生数据目录与迁移验收

- 验证日期：2026-09-09
- 验证源码提交：`c71b3b83d0dcc7a60136ad4da9d67e7f21cf35b7`
- macOS runner：macOS 26.6.2，arm64
- GitHub Actions： [macOS Native Smoke #34309093299](https://github.com/zhulongqihan/Focused-Moment/actions/runs/34309093299)
- 原始 evidence artifact： [macos-native-smoke-34309093299](https://github.com/zhulongqihan/Focused-Moment/actions/runs/34309093299/artifacts/10087780703)

## 验证范围

本次 workflow 在真实 `macos-latest` runner 上执行 `cargo test --locked --manifest-path src-tauri/Cargo.toml`，构建 unsigned debug `.app`，再运行 `scripts/macos-native-smoke.sh`。smoke 只使用 `RUNNER_TEMP` 下的合成 `HOME` 和工作目录，不读取、迁移或删除开发机/用户的真实应用数据。

## 结果

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| macOS Rust 测试 | PASS | workflow 的 `Rust tests on macOS` step 通过；storage 迁移相关测试全部通过 |
| 规范数据目录 | PASS | 真实 app 启动后使用 `~/Library/Application Support/FocusedMoment`，新启动不在工作目录创建 `FocusedMoment/` |
| 旧目录迁移 | PASS | 合成旧 `FocusedMoment/` 的 state/runtime 被复制到规范目录，state `nextRecordId=7` 与 runtime `stopwatchElapsedMs=42000` 保留 |
| 迁移前保护 | PASS | 旧目录文件迁移前后 SHA-256 不变，并保留时间戳迁移备份目录；没有删除源目录 |
| 重启读取 | PASS | 停止并重新启动 native app 后，规范目录数据可继续读取，日志没有启动恢复错误 |
| Finder/LaunchServices 风格启动 | PASS | 用 `open -n` 启动构建出的 `.app`，进程成功出现并正常结束 |
| debug `.app` 构建 | PASS | `pnpm tauri build --debug --no-sign --ci` 完成，产出 macOS `.app` 与 DMG |

第一次尝试的 run `34308083196` 暴露了 smoke 将 bundle 可执行文件误写成产品名的问题；修正为 Tauri 包名 `focused-moment` 后，run `34308536654` 通过。随后又补充了第二个合成工作目录，最终 run `34309093299` 全部通过。上述失败均不属于应用功能失败，修复与证据增强分别绑定在对应提交中。

## 边界与结论

CORE-03 的跨平台规范目录、旧目录安全发现/迁移和 macOS 原生启动/重启证据已闭环，状态改为 `DONE`。macOS 第二实例、托盘菜单点击、浮窗交互和正式安装器属于后继依赖 DESK-02，本报告不把它们冒充为已验收。

native 日志只出现 Tauri 对透明窗口未启用 `macos-private-api` 的既有提示；本次 app 启动、存储迁移和重启均未因此失败。窗口细节和托盘/浮窗行为继续由 DESK-02 的专项证据负责。
