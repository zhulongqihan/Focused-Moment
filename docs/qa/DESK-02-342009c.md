# DESK-02 · macOS 数据、窗口、单实例与安装冒烟

- 任务状态：`REVIEW`（实现和平台验收已通过，等待 v2.9.1 tag、Release 与 Universal DMG 收口）
- 任务起点：v2.9.0 发布基线 `b375f85`
- 功能验证代码基线：`342009cb68af7627739d126e3d6ba5430594f388`（版本 `2.9.1`）
- 结束时下一任务：`DATA-01`

## 交付范围

- macOS 使用 `tauri-plugin-single-instance`；第二次原生启动通过单实例回调唤回已有主窗口，不创建第二个应用实例。
- 菜单栏托盘提供“显示主界面”和“退出应用”；主窗口关闭请求默认改为隐藏，退出菜单仍可结束进程。
- 悬浮工作台继续复用待办/计时业务状态，并从命令面板提供明确入口。
- macOS native smoke 覆盖 Application Support 规范数据目录、旧目录迁移与源文件保护、不同工作目录重启、第二实例、Accessibility 窗口/菜单探测、原生 NSStatusItem 菜单点击、浮窗、LaunchServices/Finder 风格启动、隔离 DMG 挂载复制和安装后启动。

## 远程 macOS 证据

- [GitHub macOS Native Smoke run 34328199858](https://github.com/zhulongqihan/Focused-Moment/actions/runs/34328199858)：PASS，macOS 26.6.2 arm64。
- 该 run 的报告写明：规范路径、迁移/备份、跨目录重启、第二实例、浮窗、LaunchServices 启动、隔离 Applications 目录启动均 PASS。
- 原生托盘链路独立标记为 `FOCUSED_MOMENT_TRAY_NATIVE_CLICK=ok` 与 `FOCUSED_MOMENT_TRAY_MENU_SHOW_MAIN=ok`；原生 `NSStatusItem.performClick` 打开菜单，菜单首项恢复主窗口。
- Accessibility/SystemUIServer 未稳定暴露第三方状态项，因此位置探测为 `UNAVAILABLE`；这不覆盖实际托盘功能，因为原生 AppKit 点击和菜单回调均已通过。
- 下载的 run 报告：`C:\Users\yang\AppData\Local\Temp\focused-moment-macos-native-34328199858-download\macos-native-smoke-34328199858\focused-moment-macos-native-34328199858\report.md`。

## 本地与 CI 验证

- `pnpm check`：PASS。
- `pnpm build`：PASS，Vite 处理 2061 modules。
- `pnpm test:frontend -- --workers=1`：PASS，58/58；默认 8 workers 的一次运行出现 50/58，失败集中在视觉测试并发启动超时，串行重跑全部通过。
- [GitHub Checks run 34328199910](https://github.com/zhulongqihan/Focused-Moment/actions/runs/34328199910)：PASS，前端与 Rust 共 58 项通过。
- `cargo fmt --all --manifest-path src-tauri/Cargo.toml -- --check`：PASS。
- `cargo check --locked --manifest-path src-tauri/Cargo.toml`：PASS。
- `cargo test --locked --manifest-path src-tauri/Cargo.toml`：PASS，library 32/32、binary 0、doc-tests 0。
- `pnpm package:debug` 与 `pnpm package:release`：PASS；Windows release EXE、NSIS 和 MSI 均为 `2.9.1`。
- `git diff --check`：PASS。

## Windows release 产物（本地候选）

| 产物 | SHA-256 |
| --- | --- |
| `Focused Moment v2.9.1.exe` | `DC00343AE78D0CB7F5669C377FF0A0CA22BAEC345B32EAF8990079F1BF3AF29E` |
| `Focused Moment Setup v2.9.1.exe` / NSIS | `75CE141FC5D9FDE6BDBC8098FCFCB48742C0335C017B253981D131E5F5FA1CC9` |
| `Focused Moment_2.9.1_x64_en-US.msi` | `912DBF84E0F9C1CAC6E036C97450E5A1E3C40A9F1B66B26CEE7895916781B55A` |

## 边界与下一步

- macOS 安装证据使用 CI 生成的 unsigned debug DMG，挂载、复制到隔离 Applications 目录并启动通过；不声称公证、签名或真实 `/Applications` 安装通过。
- v2.9.1 tag、GitHub Release 和 Universal DMG 在本报告创建时尚未收口；收口后将把任务改为 `DONE` 并更新本报告与 `PROJECT_PLAN.md`。
- `DATA-01` 仍负责备份包含范围、跨设备搬移流程和素材清单，不被本次平台冒烟提前标记完成。
