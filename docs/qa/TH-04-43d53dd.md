# TH-04 · Aurora Ocean 五页主题验收

- 源码提交：`43d53dd`（`feat(theme): implement Aurora Ocean surfaces`）
- 发布候选：`761181d`（v2.8.0 版本同步）
- 验收日期：2026-09-09（Asia/Shanghai）
- 视觉证据：`output/qa/TH-04/761181d/`

## 实现范围

- 新增 `AuroraOceanViews.tsx` 与 `AuroraOceanViews.css`，实现今日、计时、待办、记录、设置五页。
- 通过 `ThemeSurface` 显式分派，继续复用 `MainShell` 持有的计时、待办、记录、设置 props 与真实动作。
- 在 `themes.ts` 启用 `aurora-ocean`，保留 `botanical-library` 禁用状态；无效或未实现主题仍回退到 Night Valley。
- 主题边界覆盖深海青、极光紫、潮汐青色信号、半透明圆润表面、气泡与光场背景，并隔离旧 Night Valley 的固定壳层规则。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| `pnpm check` | PASS |
| `pnpm test:frontend` | PASS，55/55；包含 Aurora 五页渲染、主题注册、共享动作和 1120/820/560/420px 压力宽度 |
| `pnpm build` | PASS，2059 modules |
| `cargo fmt --check` | PASS |
| `cargo check --locked` | PASS，focused-moment v2.8.0 |
| `cargo test --locked` | PASS，Rust library 32/32 |
| `git diff --check` | PASS |
| Windows debug/release 包 | PASS，EXE、NSIS、MSI 均生成 |
| v2.8.0 EXE 启动冒烟 | PASS，窗口标题为 `Focused Moment` |

## 视觉与边界证据

`output/qa/TH-04/761181d/` 包含 `today.png`、`focus.png`、`todos.png`、`records.png`、`settings.png` 与 `geometry.json`。标准视口为 1487×1058；五页右边界均不超过视口，最小压力尺寸还通过完整前端回归校验。

主题没有复制业务状态机；环境音/自由笔记/导出等概念图中不存在的业务能力未被伪造加入。CI/macOS Universal DMG 与 GitHub Release 属于 REL-01 的 v2.8.0 发布收口，真实 macOS 原生目录、单实例和窗口交互仍属于 CORE-03/DESK-02 的阻塞证据。
