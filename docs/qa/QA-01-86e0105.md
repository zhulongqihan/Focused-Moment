# QA-01 验证报告

- 日期：2026-09-08
- 源码提交：`86e0105`（`test: retain theme fallback and ci evidence`）
- ARCH-01 基线：`abf2bc4`
- 版本：`2.6.9`（未改版本号与已发布 Release）
- 目标：把公共业务回归、主题回退/视觉回归和 CI 失败证据纳入可复现的验证链。

## 本轮改动

- 在 `tests/today-visual.spec.mjs` 新增两项启动回归：无效 `localStorage` 主题值、已注册但未实现的 `editorial-paper` 都必须回到 Night Valley，首屏正常渲染且不出现 `.theme-surface-unavailable` 空表面。
- `.github/workflows/ci.yml` 的 Rust check/test 改用 `--locked`，使干净 checkout 使用锁定依赖；新增 GitHub Step Summary，记录提交、ref、运行链接、runner 和工具版本。
- CI 失败时上传 `test-results/`、`output/playwright/`、`output/qa/`，保留 14 天，便于从失败运行直接取得 trace、截图和专项输出；没有文件时不把上传步骤变成二次失败。

## 本地验证

- `pnpm check`：PASS。
- `pnpm test:frontend`：PASS，47/47，约 1.4 分钟；包含公共业务、五页视觉/几何、浮窗、命令面板、编辑态计时、错误态，以及本轮新增的 2 项主题回退用例。
- `pnpm build`：PASS；Vite 转换 2053 modules，保留既有大资源提示。
- `cargo fmt --check --manifest-path src-tauri/Cargo.toml`：PASS。
- `cargo check --locked --manifest-path src-tauri/Cargo.toml`：PASS。
- `cargo test --locked --manifest-path src-tauri/Cargo.toml`：PASS，library 32/32，binary 0，doc-tests 0。
- `git diff --check`：PASS。

## 证据范围与限制

- 这轮证明的是 Windows 本地 Chromium + Tauri mock、锁定依赖命令和 CI 证据配置；GitHub 真实 workflow 的新 run 需要推送后由 Actions 生成，不能把本地结果写成已完成的远程 run。
- 当前 CI 仍是 Windows runner，macOS 原生窗口/目录/单实例/安装验收继续由 CORE-03 / DESK-02 负责。
- `output/qa` 的视觉资料按现有测试生成并在失败时上传；本轮没有修改概念图、像素基线或把禁用主题标记为已实现。
- 版本、Release 资产和发布说明未变，交给 REL-01 统一收口。
