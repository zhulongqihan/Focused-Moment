# NV-06 分页视觉精修与第一套视觉验收

## 结论

NV-06 在 `a52796f` 完成。首套 Night Valley 五页在固定 fixture、Chromium + Tauri mock 下完成视觉精修与回归：Today 首屏从真实路径起点开始，只有路径在 ready 后新增节点时才自动跟随；其余四页统一使用已经随应用离线打包的 v4 山谷材质，并调整非 Today 页面亮度/饱和度。没有伪造任务、记录、统计或主题功能。

## 证据绑定

- 任务起始 SHA：`85308f1`。
- 最终源码 SHA：`a52796f`。
- 日期：2026-09-08（Asia/Shanghai）。
- 执行环境：Windows 11、Chromium、Tauri mock、Playwright 1.62.1。
- 参考视口：`1487×1058 CSS px`，`deviceScaleFactor=1`。
- 固定 fixture：日期 `2026-09-05`；7 条 45 分钟专注记录；1 条待办；无备份；不读取真实应用数据。
- 元数据：`output/qa/NV-04/a52796f/geometry.json`、`output/qa/NV-04/a52796f/scale-matrix.json`。文件由既有 NV-04 视觉 harness 生成，SHA 已绑定本轮最终源码。

## 本轮变更

1. `TodayDashboard` 记录 ready 时的节点数量，初次 ready 不再把路径强行滚到当前节点，避免首节点/标签在首屏被横向裁切；ready 后节点数量增长时仍保留自动跟随路径的行为。
2. 删除压力测试中手动设置 `scrollLeft=0` 的旁路，让测试直接验证真实首屏起点。
3. 修正两个 CSS 级联入口，使非 Today 页面实际使用 `focus-trail-atmosphere-v4.png`；同步调高普通页面和 Settings 的材质可见度。v4 已是现有离线资源，不增加外部网络依赖或新资源包。

## 五页截图

截图由最终 SHA 的完整视觉专项生成，均为 `1487×1058` PNG；文件位于被忽略的 `output/playwright/`，便于本地复核。

| 页面 | 文件 | SHA-256 |
| --- | --- | --- |
| 今日 | `output/playwright/today-after.png` | `AA832BE264CC8F7B49A5EC4942ABFA8AAFFB4976C74923ADA6546700BF763FE8` |
| 计时 | `output/playwright/night-valley-timer.png` | `854D988CCD095231A465554EEA55DEA922F72240898140D89C37E66DBDB1A69D` |
| 待办 | `output/playwright/night-valley-todo.png` | `45261F9419989E7AE20196102DD1CDA2E76C2728D6D3AA4104A008F5FB110DE4` |
| 记录 | `output/playwright/night-valley-records.png` | `6D5078B1EE6828606C5DA8DF784E5CD92458B395010647054596D1AFFF23BE9A` |
| 设置 | `output/playwright/night-valley-settings.png` | `79E50CDFFD88086E75CF346499BEDD4662D653E274EA5E42FC48CB12BE3169FE` |

目视结论：Today 首节点和首标签完整进入路径视区；计时页中央仪表与右侧操作卡仍保持层级；待办/记录/设置页获得统一的星空、山谷和路径纵深，面板文字与真实 fixture 保持可读。Records 的数据密度和 Todo 的卡片数量仍按真实 fixture 展示，不以概念图示例数据填充。

## 验证

- `pnpm check`：PASS。
- `pnpm test:frontend`：PASS，44/44，约 1.2 分钟（最终提交后复跑）。
- `pnpm exec playwright test tests/today-visual.spec.mjs --workers=1`：PASS，15/15，约 1.4 分钟。
- `pnpm build`：PASS；主 JS `639.35 kB`、CSS `255.80 kB`，保留既有 >500 kB chunk 警告。
- `git diff --check`：PASS。

## 剩余边界

- 视觉证据仍来自 Chromium + Tauri mock，不能替代 Windows WebView2 原生窗口、系统缩放、托盘/浮窗和 macOS 原生验收；CORE-03/DESK-02 的 macOS 证据继续阻塞于当前环境。
- 参考图的原始字体、分层材质和各页独立背景来源不完整；本轮统一使用已有离线 v4 资源，未宣称像素一比一或原生字体等价。
- 版本仍为 `2.6.9`，未移动发布 tag、未更新 Release 资产；版本与发布闭环由 REL-01 统一处理。
