# Focused Moment v2.0.13

## 跨午夜专注统计

- 跨过 24 点的专注记录会按实际时间拆分到对应的两天。
- 记录列表仍保留一条完整记录，不会把一次专注拆成两条记录。
- 每日专注时长、每日轮次、今日统计、连续投入天数和最佳日期都会使用拆分后的统计结果。
- 对没有完整结束时间的旧记录继续使用原有日期归类，保证历史数据兼容。

## 验证

- `pnpm check`
- `pnpm build`
- `pnpm test:frontend`（10 项通过）
- `cargo fmt --check`
- `cargo test`（11 项通过）
