# Focused Moment v2.2.2

## 修复跨窗口启动新专注时沿用旧名称

- 修复在专注悬浮窗结束或重置旧任务后，从主窗口开启新专注时，记录名称仍沿用旧任务名称的问题。
- 主窗口现在会把另一个窗口已经清空的计时上下文同步到表单；用户正在编辑的新名称不会被后台轮询覆盖。
- 增加跨窗口状态同步回归测试，覆盖旧名称清理和解锁窗口控制路径。

## 验证

- 前端类型检查、生产构建、前端自动化测试、Rust 测试与 Windows 安装包构建均通过。
