# Focused Moment v2.0.9

## 待办状态视觉区分

- 已完成待办改为绿色勾选标记、正常标题和“已完成”标签，不再使用容易和过期混淆的删除线。
- 已过期待办增加红色“已过期”标签，同时保留编辑、专注和完成操作。

## 验证

- `pnpm check`
- `pnpm build`
- `pnpm test:frontend`（8 项通过）
- `cargo fmt --check`
- `cargo test`（9 项通过）
