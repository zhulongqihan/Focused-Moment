# v1.6.0 测试记录

## 已执行
- `pnpm build`
- `cargo check --manifest-path src-tauri/Cargo.toml`

## 发布前建议手动确认
- 打开焦点页，确认“今日专注星图”不会遮挡开始、暂停、完成、重置、切模式、关联任务等核心操作。
- 验证空状态、运行中、休息中、恢复会话、窄窗下的文案与布局。
- 打开开发者页，确认版本号与里程碑显示为 `v1.6.0` / “今日专注星图版”。
- 导出完整备份并刷新备份列表，确认 `appVersion` 展示正确。
- 任务创建、编辑、完成、删除，以及复盘页范围切换、删除记录、清空范围保持可用。
