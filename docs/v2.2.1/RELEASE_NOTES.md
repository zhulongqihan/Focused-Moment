# Focused Moment v2.2.1

## 修复悬浮专注窗解锁卡死

- 修复点击悬浮专注窗的解锁按钮后，解锁按钮先消失、底层窗口仍保持鼠标穿透，导致整个窗口无法点击的问题。
- 解锁流程现在会先恢复悬浮窗的可交互状态并完成聚焦，最后再隐藏解锁按钮窗口；如果原生窗口切换出现延迟，解锁按钮会继续保留以便重试。
- 待办悬浮窗同步采用相同的安全顺序。

## 验证

- 前端类型检查、生产构建、前端自动化测试、Rust 测试与 Windows 安装包构建均通过。
