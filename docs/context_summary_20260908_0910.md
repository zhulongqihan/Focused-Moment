# Focused Moment 项目审查摘要 · 2026-09-08 09:10 +08:00

## 当前目标与交付

用户要求总结整个项目、核对进度、审查改进计划，并把增强计划放在根目录，供后续 Luna 持续执行。用户特别澄清：审查整个项目，前端只是当前阶段。

已建立根目录 `PROJECT_PLAN.md`，覆盖产品、计时、存储恢复、待办、统计、桌面多窗口、跨平台、测试、发布、文档和五套主题。它是当前持续执行入口；本摘要只是本轮历史快照。

已在 `AGENTS.md` 加入读计划/更新证据的入口；README 加入计划链接，将旧稳定版本 v2.5.0 改为实际 v2.6.9。本轮没有修改产品源码、版本号或发布资产。

## 核对后的事实

- 代码/Release 基线：`473cc6701cc6dc8608527d12e3e73bf8d2a1999e`，v2.6.9。
- 当前分支 `codex/night-valley-today`；审查开始时干净。
- 远程 main 在 `bcf23fa`，落后当前分支 9 个提交；Release tag 已指向当前功能分支提交。
- Night Valley 今日/计时/待办/记录/设置五页已接线；其他四套只有注册、五张概念图与禁用预览。
- 第一套还未通过全面行为/数据/视觉/原生验收，不能把已发布视为已全部收口。
- 旧 roadmap/项目总结/反馈/上下文多停在 v1.x/v2.0；其“当前”描述不再适用。

## 关键未解决问题

1. CORE-01/02：存储先删除主文件再 rename；主文件缺失直接默认；加载失败被吞为默认状态；存储不可用还会报告持久化成功；导入/完成先改内存、后分两文件持久化，需要故障测试和提交/回退保护。
2. CORE-04：合法 1–24 分钟倒计时重启/导入被 `.max(25分钟)` 抬高，须修复并加往返恢复测试。
3. CORE-03/DESK-02：macOS 数据路径回退 current_dir、非 Windows 单实例直接 true；能构建 DMG 不代表数据/原生行为已验证。
4. ENG-01：main/Release/CI 对齐；本地 Rust binary test 的 os error 5 待定位。
5. NV-01：外观值没有消费到渲染，默认值 Number(空串)=0，保存失败仍成功提示。
6. NV-02/03：记录固定日期、七天平均口径错误、零值柱图语义；计时七节点与四标签/三种索引不一致。
7. NV-04～06：五页固定 fixture 基线、尺寸/焦点/状态覆盖与视觉校准；不要用截图生成当验收通过。
8. DESK-03：列表编辑跳过整个轮询，计时展示停更；须分离计时和业务列表刷新。
9. ARCH/QA/PERF：MainShell 3399 行、App.css 6846 行，有不可达旧视图与多轮覆盖；先基线、再有限拆分，不复制五套业务。

这些是审查发现与待办，尚未修复。未读取用户个人数据；存储风险未被宣称为已发生的用户损失。

## 本轮验证

- `pnpm check`：通过。
- `pnpm build`：通过；主 JS 635.33 kB、CSS 254.30 kB，存在 >500 kB chunk 提示。
- `pnpm test:frontend`：32/32 通过，约 1.1 分钟，Chromium + Tauri mock。
- `cargo fmt --check`、`cargo check`（均指定 src-tauri/Cargo.toml）：通过。
- 完整 `cargo test`：Rust library 12/12 通过，随后 binary test 拒绝执行，os error 5，整命令退出 1；不记为全绿。
- GitHub main Checks run 33981366154 在旧 SHA bcf23fa 的 Rust format 失败；新分支本地格式通过。
- v2.6.9 macOS run 34148483003 成功；Release 有 Windows 安装/便携/MSI/macOS DMG 四资产。
- 两个本地带版本 EXE 的 SHA256 与 Release digest 一致；未重打包或安装。
- 检视 Today/Timer 当前截图与 Timer 参考；尚无五页全量量化结果。

## 接下来怎么做

本轮只交付审查文档。后续读取 `PROJECT_PLAN.md`，先执行 ENG-00 定位测试程序拒绝执行，再领取 CORE-01，在临时目录复现、保护有效副本，再做 CORE-02、CORE-04、ENG-01；测试环境诊断期间也可独立复现存储问题。第一套前端具体任务可按依赖在本地推进，但公开发布必须过 G0。

普通实现任务以任务卡验收通过为 DONE，可合批待发布；REL-01 单独追踪发布，避免“必须先发布才能完成实现任务”的依赖倒置。全新账户无存档应正常初始化，已有数据故障无有效恢复来源才报明确错误。

第一套通过 G1 后建立 G2 最小共享主题边界，再按 02→03→04→05 逐套完成五页、公共业务、主题切换、视觉与发布验证。

延续中文协作、保护已有改动、先读调用链再改；只在隔离夹具做损坏/恢复测试；已授权的可逆工作直接推进；计划文档不要求发空应用版本。后续产品变更继续遵守构建/包验证/提交/推送/版本与 Release 同步。
